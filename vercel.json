{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "crons": [
    {
      "path": "/api/telephony/cron",
      "schedule": "*/5 * * * *"
    }
  ],
  "buildCommand": "pnpm exec vitest run && pnpm typecheck && pnpm build"
}
