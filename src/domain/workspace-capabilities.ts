export type WorkspaceCapabilities = { notes: boolean; tasks: boolean; atomicCaseSave: boolean; pdf: boolean };
export const unavailableWorkspaceCapabilities: WorkspaceCapabilities = { notes: false, tasks: false, atomicCaseSave: false, pdf: false };
