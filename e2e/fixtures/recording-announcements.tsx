import { createRoot } from "react-dom/client";
import { AnnouncementsPanel } from "../../src/components/dispatch/settings/AnnouncementsPanel";

createRoot(document.getElementById("root")!).render(<main className="mx-auto min-w-0 max-w-6xl p-3 sm:p-5"><AnnouncementsPanel /></main>);
