## Kontrola pred merge

- [ ] Preview URL som otvoril(a) a skontroloval(a).
- [ ] Zmena neobsahuje nechcené DB migrácie, worker ani cron úpravy.
- [ ] Zmena nespôsobila živé SMS, hovory ani emaily.
- [ ] Pri produkčnom PR `dev -> main` som skontroloval(a) dev alias.
