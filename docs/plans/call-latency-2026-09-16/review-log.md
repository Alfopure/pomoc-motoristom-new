# Záznam nezávislého ralplan review — plán opravy odozvy hovorov v2 (16.–17. 9. 2026)

Štyri kolá nezávislých agentov (Architect → Critic → Planner revízia), workflow `wf_b83d5cf5-a0c`, 12 agentov. Kód overovaný na `origin/main` f8cb071 (`/tmp/pm-main`). Finálna revízia 5 (zapracovanie 4. kola orchestrátorom) je v `.omx/plans/call-latency-repair-v2-2026-09-16.md`; revízia 1 (self-review) je archivovaná ako `…draft1-self-review.md`.

## architect:v1
**Verdikt: REVISE**
**Antitéza:** Najsilnejšia alternatíva k jadru plánu: „Nerobte paralelizáciu v Node (E1.3–E1.5). Urobte E0, lean kontext (E1.1), bridge-first (E1.2), deduplikáciu opakovaných čítaní toho istého riadku session a merajte; ak p95 nesplní cieľ, choďte priamo na E2 (atomické RPC).“ Argumenty: (1) paralelizácia vnútri jednej Vercel invokácie počet round tripov neznižuje, iba ich prekrýva – reťaz `executeDial` má ≈13 sekvenčných DB volaní na člena (effects.ts:996–1062, 1064–1102, 1177) a paralelný beh ju neskráti pod ≈4+13; (2) otvára nové súbehy, ktoré dnešný kód nepozná: CAS checkpoint na jednom riadku session z viacerých vetiev (effects.ts:1035, continuation.ts:110–136), mutácia `ctx.session` z paralelných dialov, atomický dávkový INSERT vs. dnešné spracovanie 23505 per riadok (effects.ts:1119–1127); (3) RPC tieto súbehy odstraňujú v databáze, kde už žijú fence, journal a guard (`motorist_stage_transition_v1` už obsahuje rezerváciu víťaza). Slabina antitézy: E2 vyžaduje migrácie so súhlasom, kým E1.1–E1.2 + deduplikácie sú čistý kód a prinesú ≈15–20 round tripov úspory ihneď. Druhá antitéza (Telnyx-native `bridge_on_answer` pre inbound) je správne odsunutá na experiment: stratila by `park_after_unbridge: self` na zákazníckej vetve, o ktorú sa opiera lazy conference promotion (transitions.ts:2081–2094).
**Napätia:** 1) Trvanlivosť per-efekt vs. počet round tripov: checkpoint po každom efekte (effects.ts:211–222) dáva presný `databaseCursor` a lacný replay po páde; dávkový checkpoint ušetrí ≈10 round tripov, ale replay zopakuje celú dávku – bezpečné iba pre idempotentné zápisy (leg/attempt UPDATE, presence RPC s `expectedRevision`), preto `createCallbackRequest` správne ostáva samostatne. 2) Ochrana zákazníckej vetvy vs. provider auto-bridge: `bridge` vydaný na zákazníckej vetve s `park_after_unbridge: self` (transitions.ts:1216–1217) je jediná dokumentovaná ochrana pri `conference_create` z operátorovej vetvy; `bridge_on_answer` by ju presunul na operátorovu vetvu – zisk 0 round tripov medzi answer a bridge, strata dizajnu hold/consult/park. 3) Pravdivé 200 vs. Telnyx „2xx immediately“: bez workera nemožno potvrdiť a dokončiť neskôr; plán správne drží 500 pri neprevzatí (event-processor.ts:366–372), ale potom musí handler klesnúť pod `webhook_timeout_secs` 10 s, inak failover a retry zdvojujú doručenia a admisiu (≈6 round tripov na pokus). 4) Ohraničené čakanie webhookov na lease vs. priorita ovládania z PR #171: čakanie ≤1,2 s zníži búrku 500, ale predĺži držanie invokácie a súperí o lease s klikom operátora (kontroly čakajú 3 s s backoffom, session-runner.ts:454–458); má prísť až po E1.1–E1.5, keď p95 handlera klesne, inak sa iba presunie čakanie z Telnyxu do Vercelu.
**Syntéza:** Zachovať: E0 vrátane post-deploy gate 401 (cron/route.ts:27–44); E1.1 lean kontext (onOfferAnswered používa z kontextu len `settings.parkMaxMinutes`, `mediaAvailable` a `now` – transitions.ts:968–972, 1011–1013; lean vetva však musí nastaviť `mediaAvailable` z konfigurácie, nie `false` ako dnešná lean vetva session-runner.ts:222–237); E1.2 bridge-first s rozšíreným gate; E1.3 (jeden renew na skupinu je bezpečný, lebo `motorist_provider_command_prepare_v2` fencuje sám – migrácia 20260929200000:215); E1.7; E3–E5; poradie E1 → meranie → E2. Zmeniť: E1.4 – zrušenie pokusov a uvoľnenie presence porazených ostáva v kritickej dávke (paralelne, jeden checkpoint), lebo eligibilita iných hovorov číta `result = offered` a stav `ringing` (session-runner.ts:299, ring-plan eligibility); E1.5 – bez per-dial checkpointu a mutácie `ctx.session`, dávkový insert ako upsert s ignorovaním duplikátov + spätné čítanie; E1.6 – až po E1.1–E1.5; E2.1 – explicitný dizajn fence pre RPC, ktoré lease získava a zároveň zapisuje (observe_dial_v2 fencuje hlavičkami, migrácia :274) a sémantika „claim OK, lease busy = deferred“; E2.2 – guard už v stage RPC je, rozšírenie sa týka kritických zápisov a cursoru; Príloha A a ciele podľa opravených počtov (bridge dnes po ≈40, E1 reálne ≈21–22 bez ďalších deduplikácií, s nimi ≈17–18; fanout paralelne ≈17, po zoštíhlení `executeDial` ≈12); E3 – doplniť, že `appConsult`/`appAddParty` ignorujú `ownerProfileId` (transitions.ts:2330, 2405), takže mobil kolegu pri konzultácii/pridaní neobsadí jeho presence.
**Faktické chyby (11):**
1. Príloha A krok 10: `reserveAnsweredOperator` (effects.ts:1266–1321) sa na ceste kontraktu 2 nevykoná – guard beží vnútri `motorist_stage_transition_v1` (supabase/migrations/20260928110000_durable_transition_effects.sql:39–46) a `resumePendingEffects` odovzdá `guard: null` (effects.ts:1769); správny počet je 0, nie 1.
2. Príloha A krok 13: `observeParticipants` po bridge stojí 0 round tripov pri zmrazenom vypnutom nahrávaní (src/server/telephony/state/participants.ts:38–39 – skorý návrat), nie 3–4.
3. Príloha A krok 4: chýba vnorený `assertOwnership` (1 RPC `motorist_session_lease_renew_v2`) – `runSessionEvent` volá `ownedSessionWork` s existujúcim vlastníkom (session-runner.ts:433–437); správne 3, nie 2.
4. Príloha A krok 11: chýba SELECT všetkých vetiev pre dial fingerprinty pred príkazmi (effects.ts:1340–1341), +1 round trip.
5. Príloha A krok 12: `bridge` pod kontraktom 2 číta zdrojovú vetvu (effects.ts:703–706) → 6 DB round tripov, nie 5.
6. Príloha A krok 8: na čistej ceste bez pending entries a bez `presence_cancellations` je počet 0 (session-runner.ts:574–578), nie 1.
7. §2 riadok 2 / §3 N2: `executeDial` + `upsertDialedLeg` stojí ≈13–14 DB + 1 HTTP na člena (effects.ts:997, 1008, 1033, 1035, 1177; provider-journal.ts:32–36; client.ts:501; effects.ts:1067, 1081, 1085, 1087–1094, 1097; cancelled-offers.ts:12), nie 8–10.
8. §3 N9 a §5 E2.2: „guard `reserveAnsweredOperator` pred bridge (effects.ts:1266–1321)“ a „rozšírenie `motorist_stage_transition_v1` o guard-rezerváciu“ – stage RPC už guard obsahuje; E2.2 by pridávala kritické zápisy a cursor, nie guard.
9. §3 N5: `supabase.auth.getUser()` (api-auth.ts:155) je volanie na Supabase Auth, nie na PostgREST; tvrdenie „polling zdieľa pool s hovormi“ platí pre profil/org/settings/presence/snapshot, nie pre auth krok.
10. §5 E3: tvrdenie „server už cestu s vlastníkom má“ platí pre blind transfer (transitions.ts:2258–2269), ale `appConsult` (2330) a `appAddParty` (2405) `ownerProfileId` ignorujú (`profileId: null` pre číslo), takže presence kolegu sa neobsadí.
11. §5 E1.1: dnešná lean vetva vracia `mediaAvailable: false` (session-runner.ts:225); ak ju plán prevezme bez zmeny, `mohIsPlaying` (transitions.ts:968–972) vráti false a `playback_stop` sa nikdy nevydá – plán to neuvádza.
**Porušenia princípov (7):**
1. Princíp 3 (invarianty): E1.4 presúva zrušenie pokusov a uvoľnenie presence porazených do projekčnej fázy, ktorá môže ostať pending (effects.ts:1628–1636 `call_projection_pending`); eligibilita iných hovorov číta `motorist_ring_attempts.result = offered` (session-runner.ts:299) a stav `ringing` – bez pomenovaného backstopu (`call.hangup` porazených) a bez funkčného cronu ide o regresiu fairness/eligibility.
2. Princíp 3 (invarianty): E1.5 paralelný fanout bez odstránenia per-dial checkpointu (effects.ts:1035) kolíduje na CAS verzie session (continuation.ts:110–136, 3 pokusy) a môže zhodiť dial, ktorý už provider prijal, do stavu „unknown“ s replayom.
3. Princíp 3 (kompenzácie z reduceru): E1.2 bridge-first mení predpoklad kompenzačného forku – pri zlyhaní bridge ostáva MOH slučka bežať a `enterWaiting` spúšťa gather s MOH (transitions.ts:1022); kompenzácia musí obsahovať `playback_stop`.
4. Princíp 5 (hypotézy overiť skôr): E0 O2 sa opiera o hodnotu `user-agent = vercel-cron/1.0`, ktorá nie je v overených faktoch; gate E1.2 pokrýva len MOH pri ringing, nie pickup z čakania s aktívnym gather (transitions.ts:1213–1215).
5. Princíp 3 (fence): E2.1 spája získanie lease so zápismi (`observe_dial_v2` volá `motorist_telephony_fence` porovnávajúci hlavičky `x-telephony-*`, migrácia 20260929200000:274, 23–37), pričom klient generáciu ešte nepozná – plán nepopisuje interný bypass ani zmenu fence, čo je zmena invariantu bez pomenovania.
6. Princíp 4 / obmedzenia (prevádzková zmena = samostatný súhlas): E1.7 cache live-gate settings oneskorí účinok kill-switchu až o 5 s – ide o zmenu prevádzkového správania, ktorú plán neoznačuje ako rozhodnutie používateľa.
7. Princíp 2 (merateľné ciele): ciele E1 (bridge po 18–20, fanout 10 + 1 HTTP) nie sú odvodené z opravených počtov; s opísanými zmenami vychádzajú ≈21–22 a ≈17.
**Požadované zmeny (15):**
1. §Príloha A: opraviť kroky 4 (+1 vnorený `assertOwnership` renew z `runSessionEvent` → `ownedSessionWork`, session-runner.ts:433–437), 8 (0 na čistej ceste), 10 (0 – guard beží vnútri `motorist_stage_transition_v1`, `executeReduceResult` dostane `guard: null`, effects.ts:1769), 11 (+1 SELECT vetiev pre dial fingerprinty, effects.ts:1340), 12 (`bridge` = 6 DB, SELECT zdrojovej vetvy effects.ts:703–706), 13 (`observeParticipants` = 0 pri zmrazenom vypnutom nahrávaní, participants.ts:38–39) a súčet (≈60–62 + 4 HTTP, bridge po ≈40).
2. §2 riadok 2 a §3 N2: opraviť cenu `executeDial` na ≈13–14 DB + 1 HTTP na člena (renewLease :1177, assertOwnership :997, lookup :1008, authorize+checkpoint :1033–1035, renew+prepare, result, prior SELECT, upsert, usage RPC, attempt UPDATE, `cancelRevokedOffers` SELECT), nie 8–10.
3. §3 N9 a §5 E2.2: uviesť, že guard first-answer-wins na ceste kontraktu 2 je v `motorist_stage_transition_v1` (20260928110000:39–46), nie v `effects.ts:1266–1321`, a E2.2 preformulovať na pridanie kritických leg/attempt/presence zápisov a `databaseCursor` do tej istej transakcie.
4. §5 E1.4: ponechať zrušenie pokusov a uvoľnenie presence porazených v kritickej dávke (paralelne, jeden checkpoint) alebo výslovne pomenovať a otestovať backstop cez `call.hangup` porazených vetiev a závislosť od funkčného cronu.
5. §5 E1.5: odstrániť per-dial checkpoint (effects.ts:1035) a zdieľanú mutáciu `ctx.session` v paralelnej skupine; dávkový insert pokusov realizovať ako upsert s ignorovaním duplikátov a spätným čítaním so zachovaním pravidla `existing.result === "offered"` (effects.ts:1119–1127).
6. §5 E1.2: kompenzácia `bridgeId` musí pri bridge-first obsahovať `playback_stop` pred `enterWaiting` (inak beží MOH slučka aj gather MOH, transitions.ts:1022), a gate na vlastných číslach rozšíriť o pickup z čakania s aktívnym `gather` (transitions.ts:1213–1215).
7. §5 E1 ciele: doplniť deduplikácie (vnorený renew, probe z riadku `findSession`, riadok z checkpointu namiesto fresh SELECT pred prvým príkazom, SELECT vetiev len pri dial príkazoch, identita zdrojovej vetvy bridge zo snapshotu), inak cieľ „bridge po 18–20“ s opísanými zmenami vychádza ≈21–22; prepočítať cieľ.
8. §5 E1.5 cieľ: „fanout ≈10 round tripov + 1 HTTP“ podmieniť zoštíhlením `executeDial` (journal lookup len pri replay, jeden checkpoint po skupine, bez prior SELECT, cancellations zo snapshotu) alebo cieľ upraviť na ≈12–13; samotná paralelizácia dá ≈17.
9. §5 E1.6: zaradiť ohraničené čakanie webhookov až po E1.1–E1.5 a viazať na nameraný p95 handlera < 5 s, aby sa nezhoršil stret s prioritou ovládania z PR #171.
10. §5 E2.1: opísať, ako RPC, ktoré lease získava a zároveň zapisuje (`observe_dial_v2` volá `motorist_telephony_fence` s hlavičkami `x-telephony-*`, migrácia 20260929200000:274), obíde fence interne (`motorist.telephony_internal`) alebo vráti generáciu pred zápismi, a definovať sémantiku „claim OK, lease busy“ = uvoľnený claim `deferred` v tej istej transakcii; rozšíriť maticu v `tests/postgres/telephony-fencing.py`.
11. §5 E2.3 a ciele E2: dávkové dial RPC musí zahrnúť aj upsert vetvy, prepojenie pokusu a usage (dnes `upsertDialedLeg`, effects.ts:1064–1102), inak cieľ „fanout ≤ 6 + N HTTP“ neplatí.
12. §5 E0 O2: označiť hodnotu `user-agent = vercel-cron/1.0` ako neoverenú hypotézu, ktorá nie je medzi overenými faktami kontextového snapshotu.
13. §5 E1.7: cache live-gate settings označiť ako zmenu správania kill-switchu (účinok oneskorený až o 5 s) a vyžiadať rozhodnutie používateľa alebo obmedziť TTL na 1 s.
14. §5 E3: doplniť, že `appConsult` a `appAddParty` nastavujú `profileId` len pre `target.kind === "operator"` (transitions.ts:2330, 2405) a ignorujú `ownerProfileId`, takže konzultácia/pridanie na mobil kolegu neobsadí jeho presence – vyžaduje úpravu reduceru, nie len UI.
15. §3 N5: opraviť, že `auth.getUser()` ide na Supabase Auth (GoTrue), nie na PostgREST pool; pool zdieľajú až profil/organizácia/settings/presence/snapshot.

# Architektonické review plánu v2 (nezávislé, read-only)

**Verdikt: REVISE.** Jadro plánu – cron nezávislý od nasadenia, potom zúženie kritickej cesty, atomické RPC až po meraní – je správne a väčšina odkazov na kód sedí (overil som vyše 30 referencií v `/tmp/pm-main`, vrátane transitions.ts:852, 1126–1130, 1212–1218, 2095–2135, 2255–2309; effects.ts:186–259, 1147–1211, 1358–1480, 1800–1816; session-runner.ts:205–368, 433–474; event-processor.ts:236–372; client.ts:42–43, 469–543; provider-journal.ts; continuation.ts:91–136; coordinated-webphone.ts:272–311; ring-plan.ts:257–310; call-actions.ts:255–288). Zostávajú však vecné chyby v anatómii round tripov, dva nepomenované invarianty v E1.4/E1.5, chýbajúci dizajn fence pre E2.1 a ciele E1, ktoré opísané zmeny samy nedosiahnu.

## Antitéza (steelman)
„Nerobte paralelizáciu v Node (E1.3–E1.5). Urobte E0, lean kontext, bridge-first, deduplikáciu opakovaných čítaní session a merajte; ak p95 nesplní cieľ, choďte priamo na E2.“ Paralelizácia vnútri jednej invokácie počet round tripov neznižuje, iba ich prekrýva, a otvára súbehy, ktoré kód nepozná: CAS checkpoint na jednom riadku session z viacerých vetiev (effects.ts:1035, continuation.ts:110–136), mutácia `ctx.session` z paralelných dialov, atomický dávkový INSERT vs. dnešné spracovanie 23505 per riadok (effects.ts:1119–1127). RPC tieto súbehy odstraňujú tam, kde už žijú fence a journal. Slabina antitézy: E2 vyžaduje migrácie so súhlasom; E1.1–E1.2 s deduplikáciami sú čistý kód a ušetria ≈15–20 round tripov ihneď.

## Napätia
1. **Trvanlivosť per-efekt vs. round tripy.** Checkpoint po každom efekte (effects.ts:211–222) dáva presný `databaseCursor`; dávkový checkpoint ušetrí ≈10 round tripov, ale replay zopakuje celú dávku – bezpečné iba pre idempotentné zápisy. `createCallbackRequest` správne ostáva samostatne.
2. **Ochrana zákazníckej vetvy vs. auto-bridge.** `bridge` na zákazníckej vetve s `park_after_unbridge: self` (transitions.ts:1216–1217) je jediná dokumentovaná ochrana pri `conference_create` (komentár :2081–2094). Experiment namiesto základu je správne rozhodnutie.
3. **Pravdivé 200 vs. „2xx immediately“.** Bez workera plán správne drží 500 pri neprevzatí (event-processor.ts:366–372); potom však handler musí klesnúť pod 10 s, inak failover zdvojuje doručenia.
4. **Ohraničené čakanie webhookov vs. priorita ovládania (PR #171).** Čakanie 1,2 s zníži búrku 500, ale súperí o lease s klikom operátora; má prísť až po E1.1–E1.5.

## Vecné korekcie Prílohy A
- Krok 4: `runSessionEvent` volá vnorený `ownedSessionWork` → `assertOwnership` = +1 renew (session-runner.ts:433–437).
- Krok 10: guard na ceste kontraktu 2 beží **vnútri** `motorist_stage_transition_v1` (migrácia 20260928110000:39–46); `executeReduceResult` dostane `guard: null` (effects.ts:1769) → 0, nie 1. Rovnaká chyba je v E2.2 („rozšírenie o guard-rezerváciu“ – už tam je) a v N9.
- Krok 11: pred príkazmi ide SELECT vetiev pre dial fingerprinty (effects.ts:1340) = +1.
- Krok 12: `bridge` číta zdrojovú vetvu (effects.ts:703–706) = 6 DB, nie 5.
- Krok 13: `observeParticipants` pri vypnutom nahrávaní končí bez DB (participants.ts:38–39) = 0, nie 3–4.
- Krok 8: na čistej ceste 0.
- Súčet ≈ 60–62 + 4 HTTP, `bridge` HTTP po ≈ 40 – záver sedí, zloženie nie.
- §2 riadok 2 / N2: `executeDial` per člen = renewLease (:1177) + assertOwnership (:997) + lookup (:1008) + authorize + checkpoint (:1033–1035) + renew+prepare + result + prior SELECT + upsert + usage RPC + attempt UPDATE + `cancelRevokedOffers` SELECT ≈ **13–14 DB + 1 HTTP**, nie 8–10.
- N5: `auth.getUser()` ide na GoTrue, nie PostgREST.

## Invarianty a hypotézy
- **E1.4**: presun zrušenia pokusov a presence porazených do projekcie porušuje eligibilitu iných hovorov – `openOffers` čítajú `result = offered` (session-runner.ts:299) a `presenceAllowsOffer` stav `ringing`; pri zlyhaní projekcie závisí od cronu. Backstop (`call.hangup` porazených) existuje, ale nie je pomenovaný ani testovaný.
- **E1.5**: `authorizeOperatorDispatch` + checkpoint per dial (effects.ts:1033–1035) v paralelnom behu koliduje na CAS verzie; dávkový INSERT je atomický, jeden duplikát zhodí celú dávku.
- **E1.2**: pri zlyhaní `bridge` po bridge-first ostane MOH slučka bežať a `enterWaiting` spustí gather s MOH (transitions.ts:1022) – kompenzácia musí obsahovať `playback_stop`. Gate má pokryť aj pickup z čakania s aktívnym `gather` (:1213–1215). Dnešná lean vetva má `mediaAvailable: false` (session-runner.ts:225) – lean kontext pre answered ho musí brať z konfigurácie.
- **E2.1**: `observe_dial_v2` volá `motorist_telephony_fence` (migrácia 20260929200000:274), ktorý porovnáva hlavičky `x-telephony-*`; v RPC, ktoré lease iba získava, klient generáciu ešte nepozná – RPC musí interne použiť `motorist.telephony_internal` alebo vrátiť generáciu pred zápismi. „Claim OK, lease busy“ musí uvoľniť claim ako `deferred` v tej istej transakcii.
- **E0 O2**: hodnota `user-agent = vercel-cron/1.0` nie je v overených faktoch – hypotéza.
- **E1.7**: cache live-gate oneskorí kill-switch až o 5 s – rozhodnutie používateľa.
- **E3**: `appConsult`/`appAddParty` ignorujú `ownerProfileId` (transitions.ts:2330, 2405), takže mobil kolegu pri konzultácii neobsadí jeho presence.

## Dosiahnuteľnosť cieľov
- **E1 „bridge po 18–20“**: s plánom ako je vychádza ≈ 21–22; cieľ vyžaduje ešte zrušiť vnorený renew, znovupoužiť riadok z `findSession` pre probe, použiť riadok z checkpointu namiesto fresh SELECT pred prvým príkazom, vynechať SELECT vetiev bez dial príkazov a identitu zdrojovej vetvy brať zo snapshotu; potom ≈ 17–18 aj s porazenými v kritickej dávke.
- **E1 fanout „10 + 1 HTTP“**: paralelizácia sama dá ≈ 17 (4 + 13 v reťazi); 10–12 iba po zoštíhlení `executeDial`.
- **E2 „≤ 8“**: dosiahnuteľné (admit 1 + apply 1 + prepare batch 1 + deps 0–1), ale dial batch RPC musí zahrnúť aj leg upsert/attempt link/usage, inak „fanout ≤ 6“ neplatí.

## Syntéza
Zachovať E0 vrátane gate 401, E1.1, E1.2 s rozšíreným gate, E1.3 (renew per skupina je bezpečný – `prepare_v2` fencuje sám, migrácia :215), E1.7 s rozhodnutím používateľa, E3–E5 a poradie E1 → meranie → E2. Zmeniť E1.4 (porazení ostávajú kritickí, dávkovo a paralelne), E1.5 (bez per-dial checkpointu, upsert s ignorovaním duplikátov + spätné čítanie), E1.6 (až po E1.1–E1.5), E2.1/E2.2 (fence, guard), Prílohu A a ciele podľa opravených počtov, E3 o reducer pre `ownerProfileId`. Obmedzenia AGENTS.md plán dodržiava (migrácie a WAF len na súhlas, jeden cron, žiadny worker).

## critic:v1
**Verdikt: ITERATE**
**Výhrady (19):**
1. Príloha A krok 10: `reserveAnsweredOperator` (effects.ts:1266–1321) sa na kontrakte 2 nevykoná — `resumePendingEffects` odovzdáva `guard: null` (effects.ts:1769) a guard beží v `motorist_stage_transition_v1` (supabase/migrations/20260928110000_durable_transition_effects.sql:39–46); správne 0 round tripov. Rovnako preformulovať §3 N9 („stráca sa guard pred bridge“ → guard je v stage RPC) a §5 E2.2 (guard už existuje; RPC má pridať kritické leg/attempt/presence zápisy a `databaseCursor`).
2. Príloha A krok 13: `observeParticipants` pri zmrazenom vypnutom nahrávaní končí skorým návratom bez DB (src/server/telephony/state/participants.ts:38–39) → 0, nie 3–4.
3. Príloha A krok 4: `runSessionEvent` → vnorený `ownedSessionWork` s existujúcim vlastníkom volá `assertOwnership` = RPC `motorist_session_lease_renew_v2` (session-runner.ts:433–437, ownership.ts:58–65) → 3, nie 2. Krok 11: +1 SELECT vetiev pre dial fingerprinty (effects.ts:1340). Krok 12: `bridge` číta zdrojovú vetvu (effects.ts:703–706) → 6 DB. Krok 8: na čistej ceste 0 (session-runner.ts:574–578 podmienené). Súčet prepočítať (≈60–62 + 4 HTTP, bridge po ≈40).
4. §5 E1 cieľ „bridge po 18–20“: s opísanými zmenami vychádza ≈21–23 (deps 2, claim 1, findSession 1, probe+acquire+renew 3, snapshot 1, reconcile+observe 2, lean ctx 1, stage 1, fresh 1, legs SELECT 1, kritická dávka 3–4, checkpoint 1, fresh pred príkazom 1, renew+prepare 2, bridge source SELECT 1). Doplniť deduplikácie (zrušiť vnorený renew, probe z riadku `findSession`, riadok z checkpointu namiesto fresh SELECT, SELECT vetiev len pri dial príkazoch, identita zdrojovej vetvy zo snapshotu) alebo cieľ upraviť.
5. §3 N8 a §5 E1 cieľ „hangup ≈ 15“: na kontrakte 2 sa provider hangupy odosielajú už pred kritickými DB efektmi — `dispatchUrgentTeardown` (effects.ts:1686–1706) beží z `resumePendingEffects` (effects.ts:1711) hneď po stage RPC; intent `motorist_session_terminate_v2` sa zapisuje pred lease (session-runner.ts:477–481) a `reconcileTermination` (termination.ts:10–29) ich vydáva pri ďalších udalostiach. Preusporiadanie slučky v `executeReduceResult` hangup nezrýchli; pomenovať skutočnú páku (admisia ≈13 round tripov pred prvým HTTP + sekvenčná slučka teardownu → paralelizovať tam) a prepočítať cieľ.
6. §5 E1.4 opisuje status quo: member touches, call row a audit sú už dnes projekcia (`effect(…, false)` effects.ts:244–258; `deferProjections` effects.ts:1326; projekčná fáza effects.ts:1628–1631; audit effects.ts:1651). Zúžiť text na skutočnú zmenu: jeden checkpoint na kritickú dávku + paralelné nezávislé kritické zápisy; porazených ponechať v kritickej dávke (paralelne) alebo výslovne pomenovať backstop `call.hangup` porazenej vetvy (transitions.ts:1573 „offer ended“) a závislosť od funkčného cronu pri zlyhaní projekcie.
7. §5 E1.3 paralelná skupina príkazov: v slučke sa volá `persistTransition(expectedVersion: ctx.session.version)` pre `ivrDispatchOutcome` a `bindContactConference` (effects.ts:~1408–1424) a `applyPresenceChange` s `afterCommandId`; súbežné príkazy nad zdieľaným `ctx.session` skončia `SessionConflictError`. Obmedziť paralelnú skupinu na príkazy s čisto journalovým bookkeepingom (bridge bez `recordingConferenceName`, hangup, playback_stop, gather_stop), kompenzácie a checkpoint spúšťať až po `Promise.allSettled`. „Jeden renew na skupinu“ vyžaduje úpravu `prepareProviderRequest` (provider-journal.ts:32 `assertOwnership` per príkaz) — pomenovať miesto; renew tesne pred skupinou, lebo `SESSION_WORK_MS` 24 s > lease 15 s (ownership.ts:7–8).
8. §5 E1.5 paralelný fanout: odstrániť per-dial checkpoint (effects.ts:1035) a zdieľanú mutáciu `ctx.session`; dávkový INSERT pokusov je atomický a jeden duplikát zhodí celú dávku — použiť upsert s ignorovaním duplikátov a spätné čítanie so zachovaním pravidla `existing.result === "offered"` (effects.ts:1119–1127). Cena `executeDial`+`upsertDialedLeg` je ≈13–14 DB + 1 HTTP na člena (effects.ts:997, 1008, 1033–1035, 1177; provider-journal.ts:32–36; client.ts:501; effects.ts:1067, 1081, 1085, 1087–1097; cancelled-offers.ts:12), nie 8–10 — opraviť §2 riadok 2 a N2; cieľ „10 + 1 HTTP“ podmieniť zoštíhlením `executeDial` alebo upraviť na ≈12–13.
9. §5 E1.1 lean kontext: dnešná lean vetva vracia `mediaAvailable: false` (session-runner.ts:233); zdroj musí byť `config.mediaBaseUrl` (session-runner.ts:364), inak `mohIsPlaying` (transitions.ts:968–972) vráti false a `playback_stop` sa nikdy nevydá. Uviesť aj `settings.parkMaxMinutes` (transitions.ts:1011) a `recordingPolicy` z meta.
10. §5 E1.2 bridge-first: kompenzácia `bridgeId` musí obsahovať `playback_stop` pred `enterWaiting`, inak pri zlyhaní bridge beží MOH slučka aj gather MOH (transitions.ts:1021–1022); gate na vlastných číslach rozšíriť o pickup z čakania s aktívnym `gather` (transitions.ts:1213–1215).
11. §8 riadok „Round tripy“: `steps.db.count` je celkový počet DB požiadaviek requestu vrátane paralelných (request-metrics.ts:69), nie sekvenčná dĺžka reťazca pred `bridge`; kritérium „bridge po ≤ 20/≤ 8 round tripoch“ nie je z dnešných logov odvoditeľné. Doplniť `dbCountAtDispatch` do `CommandOutcome.detail`/auditu (effects.ts:140–143) alebo kritérium preformulovať na `db.count` + čas `effects_started_at → bridge dispatched`.
12. §3 N4 a §5 E1.6: odložené `call.answered/hangup/bridged` sa už dnes prehrávajú v `after()` po uvoľnení lease (`replayReadySessionEvents`, event-processor.ts:440–484, limit 2 replay, 8 s). Uviesť tento mechanizmus v N4, E1.6 zaradiť až po E1.1–E1.5 (stret s prioritou ovládania z PR #171, session-runner.ts:484) a podmieniť nameraným pomerom deliveries/events (SQL F) a p95 handlera < 5 s.
13. §5 E2.1: `motorist_provider_observe_dial_v2` volá `motorist_telephony_fence` (20260929200000:274, fence :23–37 porovnáva hlavičky `x-telephony-*` s `lease_token`/`ownership_generation`); RPC, ktoré lease získava a zároveň zapisuje, musí interne použiť `set_config('motorist.telephony_internal','1',true)` (vzor :122–126) alebo vrátiť generáciu pred zápismi; definovať „claim OK, lease busy“ = uvoľnený claim `deferred` v tej istej transakcii; rozšíriť maticu `tests/postgres/telephony-fencing.py`.
14. §5 E2.3 a ciele E2: dávkové dial RPC musí zahrnúť aj upsert vetvy, prepojenie pokusu a usage (`upsertDialedLeg`, effects.ts:1064–1102), inak „fanout ≤ 6 + N HTTP“ neplatí.
15. §5 E3: tvrdenie „server už cestu s vlastníkom má“ platí len pre blind transfer (transitions.ts:2258–2269); `appConsult` a `appAddParty` nastavujú `profileId` len pre `target.kind === "operator"` (transitions.ts:2333, 2402) a `ownerProfileId` ignorujú, takže konzultácia/pridanie na mobil kolegu neobsadí jeho presence — vyžaduje úpravu reduceru, nie len UI.
16. §5 E0 O2: hodnota `user-agent = vercel-cron/1.0` nie je medzi overenými faktami kontextového snapshotu — označiť ako hypotézu na overenie.
17. §5 E1.7: cache live-gate settings (`live_calls_enabled`, runtime.ts:69–77) oneskorí kill-switch o TTL — označiť ako zmenu správania, vyžiadať rozhodnutie používateľa alebo obmedziť TTL na 1 s; statická cache `resolveDefaultOrganizationId` je bezpečná.
18. §3 N5: `supabase.auth.getUser()` (api-auth.ts:155) ide na Supabase Auth (GoTrue), nie na PostgREST pool; pool zdieľajú až profil/organizácia/settings/presence/snapshot.
19. Príloha B / §m: posudzovaná verzia nezapracúva 15 požadovaných zmien Architekta ani sa voči nim nevymedzuje — doplniť sekciu „Zapracovanie review“ s rozhodnutím ku každej položke.
**Kontrola odkazov:** 30 overených, 7 s nálezom.
- effects.ts:1266–1321 „guard reserveAnsweredOperator pred bridge“ na kontrakte 2 — WRONG (guard: null v :1769; guard v migrácii 20260928110000:39–46)
- effects.ts:1473–1478 observeParticipants „3–4 round tripy“ — WRONG (participants.ts:38–39 skorý návrat → 0)
- Príloha A krok 4 „probe + lease_acquire = 2“ — WRONG (+1 renew: session-runner.ts:433–437 → ownership.ts:58–65)
- Príloha A krok 12 „5 DB na príkaz“ pre bridge — WRONG (effects.ts:703–706 SELECT zdrojovej vetvy → 6)
- E1.4 „call row a audit presunúť do projekčnej fázy“ — WRONG/status quo (effects.ts:244–258 effect(…,false); :1326 deferProjections; :1628–1631; :1651)
- N8 mechanizmus hangupu — INCOMPLETE (dispatchUrgentTeardown effects.ts:1686–1706/:1711; terminate_v2 session-runner.ts:477–481; termination.ts:10–29)
- E3 „server už cestu s vlastníkom má“ pre consult/add-party — WRONG (transitions.ts:2333, 2402 profileId null pre číslo)

# Kritické posúdenie plánu v2 (nezávislé, read-only, snapshot `f8cb071`)

**Verdikt: ITERATE.** Smer plánu je správny: E0 cron nezávislý od nasadenia → zúženie kritickej cesty bez migrácie → atomické RPC až po meraní; auto-bridge len ako experiment; worker vylúčený. Štruktúrne požiadavky spĺňa: princípy ↔ alternatíva A sú konzistentné, alternatívy B/C sú viabilné a D výslovne vylúčená, pre-mortem má presne 3 scenáre s prevenciou aj overením, testy pokrývajú unit/integráciu/e2e/observabilitu, kritériá majú veľkosť vzorky, obmedzenia AGENTS.md sú dodržané (migrácie a WAF len na súhlas, jeden cron, žiadny worker), čísla OpenAI sú atribuované a self-review priznaná. Posudzovaná verzia však nezapracúva 15 požadovaných zmien Architekta ani sa voči nim nevymedzuje; moje čítanie kódu jeho vecné korekcie potvrdzuje a pridáva ďalšie.

## Potvrdené korekcie Architekta
- Guard first-answer-wins na kontrakte 2 beží v `motorist_stage_transition_v1` (20260928110000:39–46); `resumePendingEffects` odovzdáva `guard: null` (effects.ts:1769), vetva `result.guard && !input.continuation` (:1266) sa nevykoná → Príloha A krok 10 = 0; N9 a E2.2 preformulovať.
- `observeParticipants` pri zmrazenom vypnutom nahrávaní končí bez DB (participants.ts:38–39) → krok 13 = 0.
- Vnorený `ownedSessionWork` volá `assertOwnership` = renew RPC (session-runner.ts:433–437, ownership.ts:58–65) → krok 4 = 3; +1 SELECT vetiev (effects.ts:1340); `bridge` = 6 DB (effects.ts:703–706).
- Lean vetva vracia `mediaAvailable: false` (session-runner.ts:233); zdroj je `config.mediaBaseUrl` (:364). Bez toho `mohIsPlaying` (transitions.ts:968–972) vráti false a `playback_stop` sa nevydá.
- `appConsult`/`appAddParty` nastavujú `profileId` len pre operátora (transitions.ts:2333, 2402); `ownerProfileId` využíva iba blind transfer (:2258–2269).

## Ďalšie nálezy
1. **Hangup (N8, cieľ „≈ 15“)**: na kontrakte 2 idú provider hangupy pred kritickými DB efektmi — `dispatchUrgentTeardown` (effects.ts:1686–1706) beží z `resumePendingEffects` (:1711) hneď po stage RPC; intent `motorist_session_terminate_v2` sa zapisuje pred lease (session-runner.ts:477–481) a `reconcileTermination` (termination.ts:10–29) ich vydáva pri ďalších udalostiach. Preusporiadanie slučky v `executeReduceResult` hangup nezrýchli; páka je admisia (≈13 round tripov pred prvým HTTP) a sekvenčná slučka teardownu.
2. **E1.4 opisuje status quo**: member touches, call row a audit sú už projekcia (`effect(…, false)` effects.ts:244–258; `deferProjections` :1326; :1628–1631; :1651). Skutočná zmena je jeden checkpoint na dávku a paralelné nezávislé kritické zápisy.
3. **E1.3 má nepomenovaný CAS hazard**: v slučke sa volá `persistTransition(expectedVersion: ctx.session.version)` pre `ivrDispatchOutcome` a `bindContactConference` (effects.ts:~1408–1424); súbežné príkazy nad zdieľaným `ctx.session` skončia `SessionConflictError`. Paralelnú skupinu obmedziť na príkazy s čisto journalovým bookkeepingom, kompenzácie až po `allSettled`; „jeden renew na skupinu“ vyžaduje zásah do `prepareProviderRequest` (provider-journal.ts:32).
4. **Kritérium §8 „bridge po ≤ 20 round tripoch“ nie je merateľné**: `steps.db.count` je celkový počet požiadaviek vrátane paralelných (request-metrics.ts:69), nie sekvenčná dĺžka pred `bridge`. Doplniť `dbCountAtDispatch` do auditu príkazov (effects.ts:140–143) alebo kritérium preformulovať.
5. **N4/E1.6**: odložené `call.answered/hangup/bridged` sa už prehrávajú v `after()` po uvoľnení lease (`replayReadySessionEvents`, event-processor.ts:440–484). Prínos ohraničeného čakania je menší, než N4 naznačuje; zaradiť po E1.1–E1.5 a podmieniť SQL F.
6. E0 O2 `user-agent = vercel-cron/1.0` označiť ako hypotézu; E1.7 cache live-gate oneskorí kill-switch o TTL — rozhodnutie používateľa alebo TTL 1 s; N5 `auth.getUser()` ide na GoTrue, nie PostgREST.

## Zadanie
Plán rieši prijatie/bridge (E1–E2), fanout (E1.5), hold/consult/add-party a mobil kolegu (E3) a hangup (N8) so zachovaním ochrán nahrávania (STOP bariéry, frozen policy, journal, fence). Po zapracovaní zmien Architekta a bodov 1–6 odporúčam schváliť; zásadné prepracovanie nie je potrebné.

## planner:v2
Revidovaný plán: 7155 slov.
**Zapracované zmeny:**
- Príloha A prepočítaná podľa overeného kódu: krok 4 = 3 (vnorený assertOwnership renew, session-runner.ts:433–437, ownership.ts:58–65), krok 8 = 0 na čistej ceste (session-runner.ts:574–578), krok 10 = 0 (guard v motorist_stage_transition_v1, 20260928110000:44–52; effects.ts:1779 guard: null, :1266 preskočenie), krok 11 +1 SELECT vetiev (effects.ts:1339–1341), krok 12 bridge = 6 (effects.ts:703–706), krok 13 = 0 (participants.ts:33–39); súčet ≈ 60–62 + 4 HTTP, bridge po ≈ 40.
- §1, §2 riadok 2, N2, N7: cena executeDial + upsertDialedLeg opravená na ≈ 13–14 DB + 1 HTTP na člena s presnými riadkami (effects.ts:997, 1008, 1029–1035, 1067, 1082, 1086, 1095, 1177; provider-journal.ts:31–36; client.ts:501; cancelled-offers.ts:12).
- N9 a alternatíva B preformulované: guard first-answer-wins beží v stage RPC pred bridge; pri auto-bridge by mohol len kompenzovať; strata park_after_unbridge: self ostáva pomenovaná.
- N5 opravené: auth.getUser() ide na Supabase Auth (GoTrue), PostgREST pool zdieľajú až profil/organizácia/settings/presence/snapshot (api-auth.ts:153–165).
- N8 prepísaný podľa skutočného mechanizmu hangupu na kontrakte 2 (terminate_v2 pred lease session-runner.ts:477–480, dispatchUrgentTeardown effects.ts:1686–1706/1711, reconcileTermination termination.ts:10–29) s odvodenou admisiou ≈ 14 round tripov pred prvým hangup HTTP a pákou (admisia + paralelný teardown).
- N4 doplnený o existujúci replay odložených faktov v after() (replayReadySessionEvents, event-processor.ts:440–484, limit 2, 8 s).
- N3 doplnený: lean vetva vracia mediaAvailable: false (session-runner.ts:233), zdroj má byť config.mediaBaseUrl (:364); vymenované jediné polia kontextu, ktoré onOfferAnswered a fork používajú.
- §4: princípy 3 a 5 rozšírené (eligibilita/presence porazených v kritickej fáze; hypotézy o infraštruktúre); pridaná alternatíva E (antitéza Architekta „bez paralelizácie v Node“) prijatá ako poradie E1a → E1b s meracím gate; alternatíva C prepočítaná (≈ 26 pred bridge).
- Etapa 1 rozdelená na E1a (bez súbehu: lean kontext s mediaAvailable z konfigurácie a settings/policy; bridge-first s kompenzáciou obsahujúcou playback_stop/gather_stop a gate rozšíreným o pickup z čakania s aktívnym gather; deduplikácie a–f; statická org cache; cache kill-switchu ako rozhodnutie používateľa; instrumentácia db_count_at_dispatch), E1b (súbeh: paralelná skupina len pre journalové príkazy s checkpointom po allSettled a renew pred skupinou v prepareProviderRequest; kritická dávka s porazenými a jedným checkpointom + pomenovaný backstop call.hangup/cron; fanout v 3 fázach bez per-dial checkpointu, bez mutácie ctx.session, upsert s ignorovaním duplikátov + spätné čítanie; paralelný teardown) a E1c (bounded wait webhookov až po E1a/E1b, viazaný na p95 < 5 s a SQL F).
- E1 ciele prepočítané z opravených počtov: E1a bridge po ≈ 26, E1b ≤ 16 (rozpis), fanout ≈ 16–17 wall-clock resp. ≈ 12–13 so zoštíhlením executeDial, hangup ≤ 10/≤ 9 pred prvým HTTP.
- E1.4 zúžená na skutočnú zmenu (projekcie sú už dnes projekčná fáza: effects.ts:243–258, 1326, 1628–1636, 1655–1665).
- E2.1 doplnená o dizajn fence pre RPC, ktoré lease získava a zapisuje (interný bypass set_config motorist.telephony_internal, vzor 20260929200000:122–126; návrat {token, generation}; observe_dial_v2 fence :274) a sémantiku „claim OK, lease busy = deferred v tej istej transakcii“; matica tests/postgres/telephony-fencing.py rozšírená.
- E2.2 preformulovaná: guard už v stage RPC; RPC pridáva kritické leg/attempt/presence zápisy a databaseCursor. E2.3: result_batch_v2 musí zahrnúť upsert vetvy, prepojenie pokusu a usage (effects.ts:1064–1101).
- E3 doplnená o úpravu reduceru: appConsult/appAddParty (transitions.ts:2333, 2402) nastavujú profileId len pre operátora; overené a spresnené, že add-party na číslo si vlastníka dorieši executeDial (effects.ts:1016–1026), consult nie.
- E0 O2: hodnota user-agent vercel-cron/1.0 označená ako neoverená hypotéza.
- §6 pre-mortem: scenár 1 rozšírený o CAS konflikt a mutáciu ctx.session, scenár 2 o gather a druhú MOH slučku, pridaný scenár lean kontextu.
- §7 testy a §8 kritériá doplnené (paralelná skupina s vylúčenými druhmi, tokeny pred dialom, teardown, reducer ownerProfileId; kritérium round tripov cez audit started_at a db_count_at_dispatch, steps.db.count ako celkový počet; hangup admisia; 0 CAS konfliktov).
- §9 ADR, §10 realizácia a §11 aktualizované podľa nového členenia etáp a rozhodnutí vyžadujúcich súhlas (cache kill-switchu).
- Poznámka k procesu nahradená: nezávislé kolo Architect/Critic prebehlo; Príloha B doplnená o datovaný záznam iterácie 2; pridaná Príloha C s rozhodnutím ku každej položke review.
- Nad rámec review overené v snapshote a doplnené: renewSessionLease na kontrakte 2 presmeruje na assertOwnership (session-runner.ts:138–139); fanout na kontrakte 2 preskakuje hromadný presence UPDATE (effects.ts:1159) a presence nastavuje authorizeOperatorDispatch per člen; ivrDispatchOutcome vracia null pre bridge/hangup/playback_stop/gather_stop (effects.ts:77–99).

## architect:v2
**Verdikt: REVISE**
**Antitéza:** Najsilnejšia alternatíva k jadru plánu (E1a → E1b súbeh v Node → E2 RPC): „E0 → instrumentácia a cena round tripu → E1a → priamo E2; z E1b ponechať iba paralelný teardown.“ Argumenty: (1) E1b odstráni ≈ 10 sekvenčných round tripov (26 → 16), ale zavádza najzložitejší súbeh celého plánu — a v databáze sa aj tak serializuje: každý fencovaný zápis do legs/attempts/presence/calls berie `FOR UPDATE` na riadok session (`20260929200000_fenced_telephony_commands.sql:27` cez triggery `:96–106`; `prepare_v2` `:216`; `motorist_presence_transition_v1` `20260928100000:83`). Zisk je iba prekrytie sieťovej latencie, nie paralelné SQL. (2) Fanoutová časť E1b.3 (A) sa bez migrácie nedá urobiť (parciálne unikátne indexy `20260903100000:361–371` PostgREST `on_conflict` nezacieli), takže „E1b bez migrácie“ je aj tak polovičné. (3) Skutočný mechanizmus — 5–6 round tripov na provider príkaz (journal prepare/renew/result + checkpoint) a checkpoint po každom efekte — odstráni až E2; súhlas s migráciou je jedna konverzácia, kým E1b je týždne práce s novými testami súbehu. (4) Cena round tripu 150–250 ms pri 1–2 ms SQL je anomália (gateway/TLS/keep-alive/pool). Ak sa dá znížiť 3–5× (napr. opakované spojenie, pool mode), 60 round tripov je 1,5–3 s a celá E1b stratí zmysel. Plán túto otázku odkladá do E5, hoci rozhoduje o hodnote E1b. Protiargument: ak používateľ migrácie neschváli, E1b je jediná cesta pod 26; paralelný teardown a skupina journalových príkazov sú nízkorizikové; plán E1b už podmieňuje meraním. Syntéza nižšie.
**Napätia:** 1. Trvanlivosť (checkpoint po každom efekte, `effects.ts:210–222`) vs. počet round tripov: per-efekt checkpoint dáva presný `databaseCursor` pri páde uprostred a kompatibilitu so staršími writermi (komentár `:213–214`); dávkový checkpoint (E1b.2) ušetrí ≈ 12 round tripov, ale replay dávky musí byť idempotentný a porazený operátor môže byť 1–2 s dlhšie „ringing“ pri páde. Obe strany sú legitímne; plán rieši idempotenciou + cronom, čo je závislosť od E0. 2. `park_after_unbridge: self` na zákazníckej vetve (`transitions.ts:1216–1218`, komentár `:2081–2094`) vs. Telnyx auto-bridge: auto-bridge dá 0 round tripov medzi answer a bridge, ale guard first-answer-wins v `motorist_stage_transition_v1` (`:44–52`) by už len kompenzoval a hold/consult/park by stratili ochranu zákazníka; správne ako experiment, nie základ. 3. Pravdivé 200 (`event-processor.ts:371`) vs. Telnyx „2xx immediately“: pravdivé 500 je jediné, čo zachová redelivery bez workera; cena je amplifikácia (148/59) pri handleri 10–30 s — riešením je zrýchliť handler (E1a/E2), nie klamať 200. 4. Ohraničené čakanie webhooku na lease (E1c) vs. priorita ovládania z PR #171 (`session-runner.ts:452–460`): čakanie zníži 500-ky, ale súperí s klikom operátora o ten istý lease a predĺži invokáciu; poradie „až po zrýchlení handlera“ je správne. 5. Nové: paralelizmus v Node vs. serializácia na zámku riadku session v DB — plán ho nepomenúva, hoci určuje reálny strop zisku E1b.
**Syntéza:** Zachovať: E0 (denylist + post-deploy gate 401), E1a ako prvý krok bez súbehu (lean kontext, bridge-first, deduplikácie a–f, org cache, instrumentácia `db_count_at_dispatch`), princípy 1–5, E1c podmienené meraním, E2 s výslovným dizajnom fence a sémantikou claim/busy, E3/E4, Prílohu A (počty sedia s kódom: 60–62, bridge ≈ 40, hangup ≈ 14). Zmeniť: (a) E1a.2 — kompenzáciu bridge NEROZŠIROVAŤ o `playback_stop`: `fork()` (`transitions.ts:136–138`) stavia kompenzáciu z pôvodného riadku (stav `ringing`/`waiting`), `mohIsPlaying(failed)` je true, `enterWaiting` (`:1020–1021`) MOH nereštartuje → `playback_stop` by dal tichú čakáreň; ponechať iba `gather_stop` pre pickup z čakania a otestovať „nový gather pri bežiacom gather“; zároveň zdokumentovať dnešnú latentnú chybu (stopMoh → bridge zlyhá → ticho). (b) E1a.1 — lean vetvu podmieniť predikátom `noContinuation` (`session-runner.ts:210–213`), lebo `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`, `recording.ts:31–33, 44, 64, 146–179`). (c) E1b — preusporiadať: najprv E1b.4 (paralelný teardown) a E1b.1 (journalová skupina, obmedzená na zmrazené vypnuté nahrávanie), E1b.2/E1b.3 až ak sa E2 neschváli; E1b.3 (A) cez N paralelných jednoriadkových INSERTov (`allSettled`), nie upsert; uviesť serializáciu na `FOR UPDATE` a ciele merať. (d) E2.1 — admit RPC musí obsahovať rozlíšenie session ako `findSession` (`event-processor.ts:81–108`) a fallback pre neznámu session/inbound `call.initiated` (`:187, 253–258`), plus `pending_commands_v2` a settings, inak „7 → 1“ a „≤ 8“ neplatí. (e) E5 — meranie ceny round tripu (SQL J) presunúť pred rozhodnutie E1b/E2 s vetvou „> 100 ms pri nízkej záťaži → gateway/keep-alive/pool pred ďalším refaktorom“.
**Faktické chyby (7):**
1. E1a.2 a §6 pre-mortem: tvrdenie, že bez `playback_stop` v kompenzácii `bridgeId` „beží pôvodná MOH slučka a `enterWaiting` spustí druhú MOH slučku“ je nesprávne — `fork()` (`transitions.ts:136–138`) vytvára kompenzáciu z pôvodného riadku session (stav `ringing`/`waiting`), takže `mohIsPlaying(failed)` (`:968–972`) vráti true, `musicRunning` je true a `enterWaiting` (`:1020–1021`) `startMoh` nevydá; pridanie `playback_stop` pred `enterWaiting` by preto zastavilo MOH bez reštartu (tichá čakáreň). Dnešný kód (stopMoh pred bridge) má z rovnakého dôvodu latentnú chybu: po zlyhaní bridge ide zákazník do čakárne bez hudby.
2. E1b.3 (A): „pokusy jedným upsertom s ignorovaním duplikátov na prirodzený kľúč“ nie je cez PostgREST realizovateľné bez migrácie — prirodzené kľúče `motorist_ring_attempts` sú parciálne unikátne indexy (`20260903100000_telnyx_telephony_foundation.sql:361–366`, `where profile_id/external_number is not null`) a cross-session index `ring_attempts_profile_open_offer_idx` (`:369–371`); PostgREST `on_conflict` nevie zacieliť parciálny index a `ignore-duplicates` bez `on_conflict` cieli PK, takže jeden duplikát zhodí celý viacriadkový INSERT — presne to, čomu sa plán chce vyhnúť.
3. E1b.1: `bridge` nie je „čisto journalový“ ani bez `recordingConferenceName` — po bridge sa volá `observeParticipants` (`effects.ts:1473–1478`), ktorý pri nahrávaní zapnutom/neistom číta `motorist_calls`, legs a intervaly a zapisuje (`participants.ts:40–60`); skorý návrat platí len pri zmrazenom vypnutom nahrávaní (`:33–39`).
4. E1b (všetky body) predpokladá nezávislé paralelné zápisy nad rôznymi riadkami; v skutočnosti každý fencovaný zápis do legs/attempts/presence/calls (triggery `20260929200000_fenced_telephony_commands.sql:96–106`) volá `motorist_telephony_fence`, ktorý berie `select … for update` na riadok session (`:27`); rovnako `prepare_v2` (`:216`), `result_v2` (`:243`), `lease_renew_v2` (`:113`) a `motorist_presence_transition_v1` (`20260928100000_atomic_presence_contract.sql:83`). Paralelné požiadavky sa v DB serializujú; zisk je iba prekrytie sieťovej latencie.
5. E2.1: „Nahrádza `event-processor.ts:236, 261`, `session-runner.ts:442, 452, 151–162, 538–543` (7 → 1)“ vynecháva, že `findSession` (`event-processor.ts:81–108`) rozlišuje session podľa sid → leg → telnyx session → conference a že pri neznámej session nasleduje `createInboundSession` (`:187`) alebo awaiting-correlation (`:253–258`); admit RPC ich musí obsahovať; `reconcileProviderEvent` (`provider-event-evidence.ts:62–66`, `motorist_provider_pending_commands_v2`) nie je v zozname, takže bez jeho zahrnutia ide o 8 → 2, nie 7 → 1.
6. E1a.5: citácia `session-runner.ts:503–507` pre `runner_started_at`/`timing` je posunutá; closure `timing` je na `:493–501` (§2 r. 5 cituje správne `:493–507`).
7. E1a.1: lean vetva pre answered nie je podmienená predikátom `noContinuation` (`session-runner.ts:210–213`); `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`), ktorá číta živú `context.recordingPolicy`, `context.announcements`, `context.line?.metadata` a `context.recordingLeaseHeld` (`recording.ts:31–33, 44, 64, 146, 156–179`) — pri nahrávaní zapnutom by zmrazená politika vydávaná ako živá zmenila rozhodnutia o nahrávaní (nie je to chyba počtov, ale chýbajúca podmienka, ktorú plán prezentuje ako hotový dizajn).
**Porušenia princípov (5):**
1. Princíp 3 (STOP bariéry a ochrany nahrávania sa nemenia) vs. E1a.1: lean kontext pre answered bez podmienky `noContinuation` podsúva `reduceRecording` zmrazenú politiku ako živú (`recording.ts:44, 64, 156–179`) — pri nahrávaných hovoroch mení rozhodnutia o `transferVerified`/`conferenceVerified` a lease; zadanie výslovne vyžaduje zachovať ochrany nahrávania.
2. Princíp 5 (hypotézy overiť pred dizajnom) vs. E1a.2: požiadavka „kompenzácia musí obsahovať `playback_stop`“ stojí na nesprávnom čítaní `fork()` (`transitions.ts:136–138`) a bez testu by zaviedla tichú čakáreň; plán ju uvádza ako overený kódový fakt.
3. Princíp 4 / obmedzenie repozitára (migrácie len na výslovný pokyn) vs. E1b.3 (A): dávkový upsert nad parciálnymi unikátnymi indexmi vyžaduje RPC (migráciu), no E1b je označená „kód, podmienená meraním“ bez migrácie.
4. Princíp 2 (optimalizuje sa počet sekvenčných round tripov, ciele merané) vs. E1b ciele „wall-clock ≈ 16–17“, „3 kolá kritickej dávky“: plán nepomenúva serializáciu na `FOR UPDATE` riadku session (`20260929200000:27`) a prezentuje wall-clock odhady ako odvodené z kódu; kód ich nepodporuje bez merania.
5. Princíp 3 (žiadne nové súbehy bez pomenovania) vs. E1b.1: skupina obsahuje `bridge`, ktorého post-dispatch `observeParticipants` (`effects.ts:1473–1478`) zapisuje intervaly pri nahrávaní; súbeh so zápismi hangupov/participant intervalov nie je pomenovaný.
**Požadované zmeny (10):**
1. E1a.2 a §6: odstrániť požiadavku pridať `playback_stop` do kompenzácie `bridgeId` (alebo ju spárovať s vynúteným `startMoh`), lebo `fork()` (`transitions.ts:136–138`) stavia kompenzáciu z pôvodného riadku a `enterWaiting` (`:1020–1021`) MOH nereštartuje; ponechať iba `gather_stop` pre pickup z čakania a do testov na vlastných číslach pridať scenár „bridge zlyhal → MOH v čakárni počuť“ a „nový gather pri bežiacom gather“.
2. §3: doplniť nový nález o latentnej chybe na main — pri dnešnom poradí `stopMoh → bridge` vedie zlyhanie bridge k `enterWaiting` bez reštartu MOH (tichá čakáreň); bridge-first ju mimochodom opraví, čo treba pomenovať a otestovať.
3. E1a.1: podmieniť lean vetvu pre `call.answered`/`call.bridged` existujúcim predikátom `noContinuation` (`session-runner.ts:210–213`: zmrazené `recording.policy.enabled === false`, žiadne recorders/barrier/pendingAudio/announcement_sequence, žiadne pending entries) a uviesť, že `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`), ktorá číta živú `context.recordingPolicy` (`recording.ts:31–33, 44, 64, 146–179`).
4. E1b.3 (A): nahradiť „jeden upsert s ignorovaním duplikátov“ N paralelnými jednoriadkovými INSERTmi pod `Promise.allSettled` s dnešnou logikou `isDuplicate → existing.result === "offered"` per člen (alebo presunúť do RPC v E2), lebo parciálne unikátne indexy (`20260903100000:361–371`) sa cez PostgREST `on_conflict` nedajú zacieliť.
5. E1b (všetky body): explicitne uviesť, že fencované zápisy a RPC berú `FOR UPDATE` na riadok session (`20260929200000:27, 113, 216, 243`; `20260928100000:83`), takže paralelná dávka/skupina/fanout sa v DB serializuje a zisk je iba prekrytie siete; ciele wall-clock označiť ako „overiť meraním“ a do `tests/postgres/telephony-fencing.py` pridať test paralelných zápisov na lock-wait/deadlock.
6. E1b.1: obmedziť paralelnú skupinu príkazov na sessions so zmrazeným `recording.policy.enabled === false` (kde `observeParticipants` končí skoro, `participants.ts:33–39`), alebo `observeParticipants` pre `bridge` spúšťať až po `allSettled`, lebo pri nahrávaní zapisuje intervaly (`effects.ts:1473–1478`, `participants.ts:40–60`).
7. E1b: preusporiadať poradie — najprv E1b.4 (paralelný teardown) a E1b.1 (journalová skupina), E1b.2 (dávkový checkpoint) a E1b.3 (fanout) až po rozhodnutí o E2; ak používateľ migrácie schváli, E1b.2/E1b.3 vynechať v prospech E2.2/E2.3.
8. E2.1: doplniť, že admit RPC musí obsahovať rozlíšenie session ekvivalentné `findSession` (`event-processor.ts:81–108`: sid → leg → telnyx session → conference) a fallback pre neznámu session/inbound `call.initiated` (`:187, 253–258`), a zahrnúť aj `motorist_provider_pending_commands_v2` (`provider-event-evidence.ts:62–66`) a čítanie settings; inak neplatí „7 → 1“ ani cieľ ≤ 8 pred bridge.
9. E5 a §5 poradie: meranie ceny round tripu (SQL J, `db.ms/db.count` pre `call.webhook`/`call.action`) presunúť hneď za E1a ako vstup do rozhodnutia E1b vs. E2 a doplniť vetvu „ak > 100 ms pri nízkej záťaži → analýza gateway/keep-alive/pool (Supabase support) pred ďalším refaktorom v Node“.
10. E1a.5: opraviť citáciu `session-runner.ts:503–507` na `:493–501` (closure `timing`).

## Nezávislé architektonické review plánu v2 (revízia 2)

**Verdikt: REVISE.** Plán je vecne silný: anatómia round tripov v Prílohe A sedí s kódom na `f8cb071` (overil som kroky 1–15: deps 2, claim 1, `findSession` 1, probe + acquire + vnorený `assertOwnership` 3, snapshot 1, reconcile + observe 2, kontext 3–4, stage + fresh 2, guard 0 v `motorist_stage_transition_v1:44–52`, kritické efekty 15 + 1, príkazy 21 + 4 HTTP, projekcie 8–9, ledger 1 → ≈ 60–62, `bridge` po ≈ 40; hangup ≈ 14 pred prvým HTTP; `executeDial` ≈ 13–14; fanout ≈ 44 + 3 HTTP). Otvoril som 30+ citovaných miest v `transitions.ts`, `effects.ts`, `session-runner.ts`, `event-processor.ts`, `client.ts`, `provider-journal.ts`, `continuation.ts`, `ownership.ts`, `coordinated-webphone.ts`, `ring-plan.ts`, `call-actions.ts`, migráciách a dokumentácii; s výnimkou jednej posunutej citácie sedia. Ostávajú však štyri chyby dizajnu, z toho dve by pri implementácii spôsobili regresiu.

### Faktické chyby s dopadom
1. **E1a.2 kompenzácia bridge.** Plán vyžaduje `playback_stop` pred `enterWaiting`, lebo „inak beží pôvodná MOH slučka a spustí sa druhá“. Kód hovorí opak: `fork()` (`transitions.ts:136–138`) vytvára kompenzáciu z **pôvodného** riadku session (stav `ringing`/`waiting`), takže `mohIsPlaying(failed)` je true a `enterWaiting` (`:1020–1021`) `startMoh` nevydá. Pri bridge-first teda MOH po zlyhaní bridge správne pokračuje; pridanie `playback_stop` by dalo **tichú čakáreň**. Vedľajší nález: dnešné poradie `stopMoh → bridge` má z rovnakého dôvodu latentnú chybu (po zlyhaní bridge ide zákazník do čakárne bez hudby) — bridge-first ju mimochodom opraví.
2. **E1b.3 (A) dávkový upsert pokusov.** `motorist_ring_attempts` má parciálne unikátne indexy (`20260903100000:361–366`) a cross-session index `profile_id where result='offered'` (`:369–371`). PostgREST `on_conflict` parciálny index nezacieli; `ignore-duplicates` bez `on_conflict` cieli PK → jeden duplikát zhodí celý INSERT. Bez migrácie sú možné len N paralelné jednoriadkové INSERTy.
3. **E1b.1 „čisto journalový“ bridge.** Po `bridge` beží `observeParticipants` (`effects.ts:1473–1478`), ktorý pri nahrávaní číta a zapisuje intervaly (`participants.ts:40–60`); skorý návrat platí len pri zmrazenom vypnutom nahrávaní (`:33–39`).
4. **E2.1 „7 → 1“.** Admit RPC musí prevziať rozlíšenie session z `findSession` (`event-processor.ts:81–108`) a fallback pre neznámu session/inbound `call.initiated` (`:187, 253–258`); `pending_commands_v2` (`provider-event-evidence.ts:62–66`) v zozname chýba.

### Chýbajúca mitigácia invariantu
**E1a.1 lean kontext** nie je podmienený predikátom `noContinuation` (`session-runner.ts:210–213`). `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`), ktorá číta živú `context.recordingPolicy`, `announcements`, `line` (`recording.ts:31–33, 44, 64, 146–179`). Bez podmienky by zmrazená politika vydávaná ako živá menila rozhodnutia `transferVerified`/`conferenceVerified` — porušenie zadania „ochrany nahrávania ostávajú“.

### Nepomenované napätie: Node paralelizmus vs. zámok riadku session
Každý fencovaný zápis do legs/attempts/presence/calls (triggery `20260929200000:96–106`) volá `motorist_telephony_fence` so `select … for update` na riadok session (`:27`); rovnako `prepare_v2` (`:216`), `result_v2` (`:243`), `lease_renew_v2` (`:113`) a `motorist_presence_transition_v1` (`20260928100000:83`). Paralelná dávka E1b.2, skupina E1b.1 aj fanout E1b.3 sa v DB **serializujú**; zisk je iba prekrytie sieťovej latencie (ktorá je síce dominantná, 150–250 ms vs. 1–2 ms SQL, ale to je práve nezmeraná hypotéza N6). Deadlock nehrozí (poradie child → session je jednotné), no ciele „3 kolá“ a „16–17 wall-clock“ sú odhady, nie odvodenia z kódu.

### Antitéza a syntéza
Najsilnejšia alternatíva: **E0 → instrumentácia + cena round tripu → E1a → priamo E2; z E1b len paralelný teardown.** E1b ušetrí ≈ 10 round tripov za cenu najzložitejšieho súbehu, časť potrebuje migráciu aj tak, a skutočný mechanizmus (5–6 round tripov na príkaz, checkpoint per efekt) odstráni až E2. Ak je cena round tripu anomália gateway/keep-alive/pool, jej oprava zníži všetko 3–5× a E1b stratí zmysel — preto meranie SQL J patrí pred rozhodnutie o E1b, nie do E5. Protiargument (bez súhlasu s migráciou je E1b jediná cesta pod 26; teardown a journalová skupina sú bezpečné) prijímam ako **poradie**: E1b.4 a E1b.1 (obmedzená na vypnuté nahrávanie) skoro, E1b.2/E1b.3 len ak sa E2 neschváli.

### Realizovateľnosť cieľov
- **E1a ≈ 26 pred bridge, hangup ≤ 10**: dosiahnuteľné, aritmetika sedí s kódom (dedupe a–f sú realizovateľné; (d) sa opiera o RETURNING fencovaného checkpointu, ktorý nesie aj `termination_requested_at`, `continuation.ts:110–111`).
- **E1b ≤ 16 pred bridge, fanout 16–17 (12–13)**: aritmeticky áno, wall-clock len po meraní (serializácia na zámku); fanout „10 + 1 HTTP“ bez E2 nie.
- **E2 ≤ 8**: dosiahnuteľné iba ak admit RPC pohltí aj `pending_commands_v2`, settings a rozlíšenie session (deps 1 + admit 1 + apply_critical 1 + prepare_batch 1 ≈ 4–5).

### Ostatné overenia
Telnyx tvrdenia zodpovedajú faktom v snapshote (retry 5xx/408/429, failover, `prevent_double_bridge`, žiadny výrok o playback pri bridge); hypotézy (MOH pri bridge, auto-bridge, cron user-agent, `getClaims`) sú správne označené. Schvaľovacia matica je primeraná (WAF, migrácie, kill-switch cache, Telnyx timeout, platené PSTN vetvy); E1b.3 (A) by však skryto vyžadovala migráciu — treba preznačiť alebo zmeniť. Repo obmedzenia (dev-first, jeden cron, žiadny worker, iba táto kópia) plán rešpektuje. Minor: E1a.5 cituje `session-runner.ts:503–507`, správne `:493–501`.

Po zapracovaní požadovaných zmien (10 položiek, každá s odkazom na sekciu) odporúčam APPROVE.

## critic:v2
**Verdikt: ITERATE**
**Výhrady (15):**
1. E1a.2 + §6: kompenzácia `bridgeId` nesmie obsahovať `playback_stop` — `fork()` (`transitions.ts:136–138`) stavia kompenzáciu z pôvodného riadku (stav ringing/waiting), `mohIsPlaying(failed)` je true, `enterWaiting` (`:1020–1021`) `startMoh` nevydá → tichá čakáreň. Ponechať iba `gather_stop` pre pickup z čakania; do §3 doplniť latentnú chybu dnešného poradia (stopMoh → bridge → zlyhanie → čakáreň bez MOH), ktorú bridge-first opraví; do testov na vlastných číslach pridať „bridge zlyhal → MOH v čakárni počuť“ a „nový gather pri bežiacom gather“.
2. E1a.2: uviesť, že pri odmietnutom bridge kontinuácia vyradí VŠETKY príkazy záznamu (`effects.ts:1583` `completedCommands = commands.map(commandKey)`), takže pri bridge-first sa zvyšné `playback_stop`/`gather_stop`/hangupy porazených z tohto záznamu už nepošlú — kompenzácia musí niesť to, čo je nutné, a plán to má výslovne pomenovať.
3. E1a.1: lean vetvu podmieniť existujúcim predikátom `noContinuation` (`session-runner.ts:210–213`: zmrazené `recording.policy.enabled === false`, žiadne recorders/barrier/pendingAudio/announcement_sequence/pending entries); `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`) a `eligible`/`startBeforeAudio`/`requiresRecordingLease` čítajú živú `context.recordingPolicy` (`recording.ts:44, 57–66, 154–160`) — bez podmienky zmrazená politika vydávaná ako živá mení nahrávacie rozhodnutia (porušenie princípu 3 a zadania).
4. E1b.3 (A): „jeden upsert s ignorovaním duplikátov“ nie je cez PostgREST realizovateľný — prirodzené kľúče `motorist_ring_attempts` sú parciálne unikátne indexy a cross-session `ring_attempts_profile_open_offer_idx` (`20260903100000:361–371`); `ignore-duplicates` bez `on_conflict` cieli PK, jeden duplikát (aj „už ponúkaný v inej session“) zhodí celý viacriadkový INSERT. Nahradiť N paralelnými jednoriadkovými INSERTmi pod `Promise.allSettled` s dnešnou logikou `existing.result === "offered"` (`effects.ts:1120–1126`) alebo presunúť do RPC v E2 a preznačiť ako vyžadujúce migráciu.
5. E1b (všetky body): pomenovať, že každý fencovaný zápis do legs/attempts/presence/calls volá `motorist_telephony_fence` so `select … for update` na riadok session (`20260929200000:27`, triggery `:96–106`), rovnako `prepare_v2` (`:216`), `result_v2` (`:243`), `lease_renew_v2` (UPDATE riadku) a `motorist_presence_transition_v1` (`20260928100000:83`) — paralelná dávka/skupina/fanout sa v DB serializuje, zisk je iba prekrytie siete (nezmerané, N6). Ciele wall-clock označiť „overiť meraním“, do `tests/postgres/telephony-fencing.py` pridať test paralelných zápisov na lock-wait/deadlock a uviesť spotrebu PostgREST poolu súbežnými požiadavkami popri pollingu (N5).
6. E1b.1: `bridge` nie je „čisto journalový“ — po ňom beží `observeParticipants` (`effects.ts:1473–1478`), ktorý pri nahrávaní zapnutom/neistom číta a zapisuje intervaly (`participants.ts:40–60`); skorý návrat platí len pri zmrazenom vypnutom nahrávaní (`:33–39`). Obmedziť paralelnú skupinu na sessions s `noContinuation` alebo observáciu spúšťať až po `allSettled`.
7. E1b poradie: najprv E1b.4 (paralelný teardown) a E1b.1 (journalová skupina), E1b.2 (dávkový checkpoint) a E1b.3 (fanout) až po rozhodnutí o E2; ak používateľ migrácie schváli, E1b.2/E1b.3 vynechať v prospech E2.2/E2.3.
8. E2.1: „7 → 1“ vynecháva rozlíšenie session ekvivalentné `findSession` (`event-processor.ts:81–108`: sid → leg → telnyx session → conference), fallback pre neznámu session/inbound `call.initiated` (`:187, 253–258`), `motorist_provider_pending_commands_v2` (`provider-event-evidence.ts:62–66`) a čítanie settings; bez ich zahrnutia ide o 8 → 2 a cieľ ≤ 8 pred bridge neplatí.
9. E5/§5: meranie ceny round tripu (SQL J, `db.ms/db.count` pre `call.webhook`/`call.action`) presunúť hneď za E1a ako vstup rozhodnutia E1b vs. E2; doplniť vetvu „> 100 ms pri nízkej záťaži → analýza gateway/keep-alive/pool (Supabase support) pred ďalším refaktorom v Node“.
10. E1a.5: citácia `session-runner.ts:503–507` je posunutá; closure `timing` je na `:493–501` (riadky 503–507 sú log lease/maxRetries).
11. §6 pre-mortem: formát vyžaduje presne tri scenáre, plán má štyri — zlúčiť „lean kontext vynechá závislosť“ do scenára nahrávacích invariantov alebo scenár WAF presunúť do stop/rollback etapy 0.
12. §8 kritérium round tripov: `steps.db.count` „dnes ≈ 60–62“ podhodnocuje — `db` krok meria každú PostgREST požiadavku vrátane paralelných (`src/lib/supabase/admin.ts:13–14` → `telephonyDatabaseFetch`, `request-metrics.ts:22–29`): snapshot = 3, kontext ≈ 8, call row 2 … dnes ≈ 70. Ciele ≤ 30/≤ 15 odvodiť znova z počtu požiadaviek alebo formulovať len cez `db_count_at_dispatch`/`commands[bridge].started_at`.
13. E1a.3 (d): uviesť dôvod bezpečnosti — fresh SELECT pred príkazom (`effects.ts:1365`) možno vynechať len preto, že `prepare_v2` sám blokuje nové príkazy po `termination_requested_at`/`ended_at` (`20260929200000:227–230`); inak `commandStillApplies` stráca kontrolu ukončenia.
14. E3 consult na osobné číslo kolegu: ak `authorizeOperatorDispatch` (`effects.ts:1029`) obsadí presence kolegu pre vetvu s rolou `consult`, plán neuvádza, kto ju uvoľní pri konci konzultácie/hangupe (token sa viaže cez `transitions.ts:202–210` `presenceChange`); doplniť mitigáciu a test.
15. Príloha B/C: po zapracovaní doplniť záznam iterácie 3 (Architect REVISE → Critic ITERATE) s rozhodnutím ku každej položke.
**Kontrola odkazov:** 40 overených, 1 s nálezom.
- src/server/telephony/session-runner.ts:493–501 closure timing — OK; citácia E1a.5 `:503–507` — WRONG (posunutá)

## Nezávislé kritické review plánu v2 (revízia 2)

**Verdikt: ITERATE.** Plán je vecne najsilnejší z troch dokumentov: anatómia round tripov (Príloha A) sedí s kódom na `f8cb071` — overil som kroky 1–15 vrátane vnoreného renew (`session-runner.ts:433–437`), guardu v `motorist_stage_transition_v1:44–52`, checkpointu po každom efekte (`effects.ts:207–222`) a 5–6 round tripov na príkaz (`effects.ts:1365`, `provider-journal.ts:31–36`, `client.ts:488–501`, `effects.ts:703–706, 1480`). Etapovanie E0 → E1a → meranie → E1b/E2, alternatívy A–E, schvaľovacia matica a repo obmedzenia obstáli. Plán však vznikol pred nezávislým review Architekta a jeho desať požiadaviek nezapracúva; sedem som znovu overil čítaním snapshotu a **všetky platia**. Pridávam štyri vlastné nálezy.

### Potvrdené chyby Architekta (blokujúce)
1. **E1a.2 kompenzácia `playback_stop`** je nesprávna: `fork()` (`transitions.ts:136–138`) stavia kompenzáciu z pôvodného riadku (stav `ringing`/`waiting`), `mohIsPlaying(failed)` je true a `enterWaiting` (`:1020–1021`) `startMoh` nevydá — pridaný `playback_stop` znamená tichú čakáreň. Doplnok: pri odmietnutom bridge kontinuácia **vyradí všetky príkazy záznamu** (`effects.ts:1583`), takže pri bridge-first sa zvyšné stop príkazy a hangupy porazených z tohto záznamu už nepošlú; kompenzácia musí niesť len nutné (`gather_stop` pri pickupe z čakania) a §3 má pomenovať latentnú chybu dnešného poradia (stop → bridge → zlyhanie → čakáreň bez hudby).
2. **E1a.1 lean kontext bez predikátu `noContinuation`** (`session-runner.ts:210–213`): `reduce` beží cez `reduceRecording` (`transitions.ts:507–509`), `eligible`/`startBeforeAudio` čítajú živú `context.recordingPolicy` (`recording.ts:44, 154–160`) a `requiresRecordingLease` (`:57–66`) rozhoduje po reduceri. Zmrazená politika vydávaná ako živá mení nahrávacie rozhodnutia — porušenie princípu 3 a zadania „ochrany nahrávania ostávajú“.
3. **E1b.3 (A) dávkový upsert** nie je cez PostgREST realizovateľný: kľúče sú parciálne unikátne indexy a cross-session `ring_attempts_profile_open_offer_idx` (`20260903100000:361–371`); `ignore-duplicates` cieli PK, jeden duplikát (aj „už ponúkaný v inej session“) zhodí celý INSERT. Bez migrácie iba N paralelných jednoriadkových INSERTov s logikou `existing.result === "offered"` (`effects.ts:1120–1126`).
4. **E1b serializácia v DB**: každý fencovaný zápis volá `motorist_telephony_fence` so `select … for update` na riadok session (`20260929200000:27`, triggery `:96–106`), rovnako `prepare_v2` (`:216`), `result_v2` (`:243`) a presence RPC (`20260928100000:83`). Paralelná dávka/skupina/fanout sa serializuje; zisk je prekrytie siete (nezmerané, N6). Ciele wall-clock označiť „overiť meraním“, doplniť lock-wait test do `telephony-fencing.py` a spotrebu poolu súbežnými požiadavkami.
5. **E1b.1 `bridge` nie je čisto journalový**: nasleduje `observeParticipants` (`effects.ts:1473–1478`) so zápismi intervalov mimo zmrazeného vypnutého nahrávania (`participants.ts:33–60`). Obmedziť skupinu na `noContinuation` sessions alebo observáciu presunúť za `allSettled`.
6. **E2.1 „7 → 1“** vynecháva rozlíšenie session (`event-processor.ts:81–108`), fallback `call.initiated`/awaiting-correlation (`:187, 253–258`) a `pending_commands_v2` (`provider-event-evidence.ts:62–66`); reálne 8 → 2, kým RPC tieto kroky nepohltí.
7. Poradie E1b (teardown a journalová skupina skôr; dávka a fanout až po rozhodnutí o E2), meranie ceny round tripu (SQL J) hneď po E1a, citácia E1a.5 správne `session-runner.ts:493–501`.

### Vlastné nálezy
8. **§6 má štyri scenáre**, formát vyžaduje presne tri: zlúčiť „lean kontext“ do scenára nahrávacích invariantov alebo WAF presunúť do stop/rollback E0.
9. **§8 `steps.db.count` „dnes ≈ 60–62“ podhodnocuje**: `db` meria každú PostgREST požiadavku vrátane paralelných (`admin.ts:13–14` → `telephonyDatabaseFetch`); snapshot 3, kontext ≈ 8, call row 2 … dnes ≈ 70. Ciele ≤ 30/≤ 15 odvodiť znova alebo formulovať iba cez `db_count_at_dispatch`/`started_at`.
10. **E1a.3 (d)** je bezpečné len preto, že `prepare_v2` blokuje nové príkazy po `termination_requested_at` (`20260929200000:227–230`); bez tejto vety `commandStillApplies` stráca kontrolu ukončenia — uviesť ako dôvod.
11. **E3 consult na osobné číslo**: ak `authorizeOperatorDispatch` obsadí presence kolegu pre vetvu `consult`, plán neuvádza, kto ju uvoľní (koniec konzultácie/hangup; token sa viaže cez `transitions.ts:202–210`) — doplniť mitigáciu a test.

### Čo obstálo
Alternatívy (A základ, C ako E0/E1a, E ako poradie, B experiment, D neplatné), kritériá s veľkosťami vzoriek (≥ 30, ≥ 20, 24 h, 10 min × 2), testovací plán unit/integrácia/e2e/observabilita, dev-first a jediný cron, žiadny odkaz na pôvodný produkčný projekt, hypotézy označené (MOH pri bridge, auto-bridge, user-agent cronu, `getClaims`, cena round tripu), čísla OpenAI pripísané, self-review zverejnený. Nálezy N1–N9 vrátane mechanizmu hangupu (`dispatchUrgentTeardown`, `effects.ts:1686–1711`) a `getUser` na GoTrue (`api-auth.ts:153–157`) sedia.

Po zapracovaní bodov 1–11 (každý s odkazom na sekciu a záznamom v Prílohe B/C ako iterácia 3) odporúčam APPROVE.

## planner:v3
Revidovaný plán: 10177 slov.
**Zapracované zmeny:**
- Hlavička a poznámka k procesu: revízia 3 (17. 9.), slučka je nezávislá (iterácie 2 a 3), self-review z it. 1 len ako historický záznam v Prílohe B.
- E1a.2 a §6: odstránená požiadavka pridať `playback_stop` do kompenzácie bridge (overené: `fork()` `transitions.ts:136–138` stavia kompenzáciu z pôvodného riadku, `enterWaiting` `:1001, 1020–1021` MOH nereštartuje); ponechaný `gather_stop` len pri pickupe z čakania; kompenzácia výslovne nesie nutné príkazy, lebo `effects.ts:1585` vyradí všetky príkazy záznamu; hangupy porazených ako odporúčanie; nové testy (bridge zlyhal → MOH v čakárni, nový gather pri bežiacom gather, porazení prestanú zvoniť).
- §3: nové nálezy N10 (latentná tichá čakáreň pri dnešnom poradí stopMoh → bridge) a N11 (serializácia paralelných zápisov na `FOR UPDATE` riadku session: `20260929200000:27, 92–107, 113, 135–136, 215–216, 243`; `20260928100000:83`), N3/N5/N6 doplnené (noContinuation, pool, rozhodovacia úloha ceny round tripu).
- E1a.1 a N3: lean kontext podmienený existujúcim predikátom `noContinuation` (`session-runner.ts:209–213`) s odôvodnením cez `reduce → reduceRecording` (`transitions.ts:507–509`, `recording.ts:31–33, 44, 57–66, 146–179`); `announcements` a `line: null` odôvodnené (`recording.ts:29–30`).
- E1a.3 (d): doplnený dôvod bezpečnosti (`prepare_v2` blokuje príkazy po terminácii `:227–230`, `commandStillApplies` `continuation.ts:50–59`) a nová podmienka — klient musí PT409 správu terminácie mapovať na tiché preskočenie (dnes `ownership.ts:52` ju prepustí ako generickú chybu → incident `effects.ts:1549–1550`).
- E1a.5 a §2 r. 5: citácia opravená na presný rozsah `session-runner.ts:494–506` (`runnerStarted` `:495`, closure `timing` `:501–506`); vysvetlené, že `db_count_at_dispatch` počíta požiadavky vrátane paralelného snapshotu (+2).
- Nová etapa E1m: meranie ceny round tripu (SQL J, A/D/I) hneď po E1a s rozhodovacími vetvami (> 100 ms → analýza gateway/keep-alive/pool; p95 ≤ 2 s → stop; inak rozhodnutie používateľa o E2); E5 a §8 naň odkazujú.
- E1b preusporiadaná: E1b-1 (paralelný teardown, journalová skupina len pod `noContinuation` alebo s `observeParticipants` po `allSettled` — `effects.ts:1473–1478`, `participants.ts:38–39`) vždy; E1b-2 (dávkový checkpoint, fanout) len ak sa E2 neschváli; spoločné pravidlo N11, wall-clock ciele označené „overiť meraním“, nový lock-wait/deadlock test v `tests/postgres/telephony-fencing.py`, kompatibilita kurzora so staršími writermi (`effects.ts:213–214`).
- E1b.4 (A): dávkový upsert nahradený N paralelnými jednoriadkovými INSERTmi pod `Promise.allSettled` s logikou `existing.result === "offered"` (`effects.ts:1120–1126`), lebo parciálne unikátne indexy (`20260903100000:360–371`) nie sú cez PostgREST `on_conflict` dosiahnuteľné; RPC variant presunutý do E2.3.
- E2.1: admit RPC obsahuje rozlíšenie session ekvivalentné `findSession` (`event-processor.ts:81–108`), `pending_commands_v2` a settings; fallbacky `createInboundSession` (`:270–271`, `:187`), awaiting-correlation (`:274–280`) a conference dôkaz cez HTTP GET (`provider-event-evidence.ts:72–79`) ostávajú mimo; počet 8 → 2 namiesto 7 → 1; cieľ pred bridge prepočítaný (≈ 5–6, rezerva voči ≤ 8 podmienená). E2.3 doplnený o dávkový INSERT pokusov.
- E3: doplnený životný cyklus presence konzultovaného kolegu (dispatch `effects.ts:1029–1034`, guard `transitions.ts:1271–1276`, neprijatie `:1572–1574`, zrušenie `:2366`, odchod `:1615`, dokončenie `:1690–1701` → `releaseTalkingOperators` `:1494`) s testom piatich koncov.
- §4: nová alternatíva F (antitéza Architekta it. 3) prijatá s výhradou; princípy 2, 3 a 5 preformulované (wall-clock nie je odvodený z kódu; lean kontext a skupiny len pod noContinuation; žiadna požiadavka na nesprávnom čítaní kódu); kritérium 2 rozšírené o lock-wait/deadlock.
- §5 poradie etáp výslovne: E0 → E1a → E1m → E1b-1 → (E2 | E1b-2) → E1c → E3 → E4; E0 doplnená o riziko/prevenciu WAF presunuté z pre-mortem.
- §6: presne tri scenáre (súbeh vrátane lock-wait; bridge-first vrátane N10 a vyradenia príkazov; lean kontext zlúčený s nahrávacími invariantmi).
- §7: nové testy (mapovanie PT409 terminácie, lean kontext len pod noContinuation, paralelné zápisy lock-wait/deadlock, admit bez session nezapíše nič, päť koncov presence konzultácie, simulované zlyhanie bridge v živom okne).
- §8: kritérium round tripov znovu odvodené — `steps.db.count` dnes ≈ 70 požiadaviek (sekvenčné 60–62 + paralelné), ciele ≤ 56 / ≤ 50 / ≤ 16 s aritmetikou; reťazec pred bridge cez `db_count_at_dispatch` ≤ 28 / ≤ 18 / ≤ 10; nový riadok cena round tripu; bezpečnosť rozšírená o deadlocky a tichú čakáreň.
- §9–§11, §10 realizácia: zosúladené s novým poradím (E1m, E1b-1/E1b-2, E2 preferovaná), doplnené drivers N10/N11/N6 a follow-up hangupov porazených.
- Príloha A: doplnené poznámky o zámku riadku session (#11), `observeParticipants` po bridge (#12/#13), počet požiadaviek vrátane paralelných (≈ 70) v súčte; ciele prepísané (E1b-2 len bez E2).
- Príloha B: nový datovaný záznam iterácie 3 s výpočtom zmien; Príloha C: nová tabuľka iterácie 3 s rozhodnutím ku každej položke Architekta a Critica vrátane opravených nepresných citácií recenzentov (`:1585` namiesto `:1583`, `lease_renew_v2` `:130–140` namiesto `:113`, awaiting-correlation `:274–280` namiesto `:253–258`, timing closure `:501–506`).
**Odmietnuté po overení:**
- Architect (antitéza it. 3), časť „z E1b ponechať iba paralelný teardown“ a E1b.2/E1b.3 úplne vypustiť: odmietnuté čiastočne — E1b-2 (dávkový checkpoint, fanout) ostáva ako výslovná záloha len pre prípad, že používateľ migrácie E2 neschváli (Architect sám uvádza, že vtedy je to jediná cesta pod ≈ 26), a journalová skupina ostáva v E1b-1, lebo pod `noContinuation` je nízkoriziková a prekryje 3 sekvenčné HTTP po 5 s; poradie a podmienenosť podľa Architekta sú prijaté.
- Critic/Architect citácia `session-runner.ts:493–501` pre closure `timing`: prijatá oprava, ale s presným rozsahom `:494–506` (`runnerStarted` `:495`, closure `:501–506`) — navrhovaný rozsah `:493–501` closure nepokrýva (riadok 493 je prázdny, closure končí na 506).
- Critic citácia `effects.ts:1583` pre `completedCommands = commands.map(commandKey)`: nález prijatý, riadok je `effects.ts:1585` (1583 je komentár).
- Architect citácia `lease_renew_v2` (`:113`) a awaiting-correlation (`:253–258`): nálezy prijaté, ale `:113` je `lease_acquire_v2` (renew je `:130–140`, UPDATE riadku `:135–136`) a awaiting-correlation je `event-processor.ts:274–280` (`:253–255` je claim busy); plán uvádza presné riadky.

## architect:v3
**Verdikt: REVISE**
**Antitéza:** Najsilnejšia alternatíva k jadru plánu: „E0 → E1a (nutný refaktor aj pre RPC) → rovno žiadosť o migrácie E2; E1m nie je gate, ale kontrola; E1b-1 aj E1b-2 vypustiť.“ Argumenty: (1) E1a odstraňuje len duplicitné čítania (−14), mechanizmus – 5–6 round tripov na príkaz a checkpoint po každom efekte – ostáva; pri 150–250 ms je 26 round tripov 4–6,5 s, stále mimo cieľa 2 s, takže výsledok E1m je pri dnešnej cene round tripu predvídateľný. (2) Súbeh v Node (E1b) sa v DB serializuje na `select … for update` riadku session (`20260929200000:27, 113, 216, 243`; `20260928100000:83`), platí sa N spojeniami poolu popri pollingu a otvára nové režimy zlyhania (CAS checkpointu, mutácia `ctx.session`, duplicitný INSERT) – teda najzložitejší kód s najmenej istým ziskom. (3) Súhlas s migráciou je jedna konverzácia; udržiavať dve paralelne navrhnuté cesty (E1b-2 vs. E2.2/E2.3) zdvojnásobuje testovaciu maticu. Protiargumenty (prečo plán antitézu neprijíma úplne): ak používateľ migrácie odmietne, E1b-2 je jediná cesta pod 26; ak E1m ukáže, že cena round tripu je anomália gateway/poolu (SQL 1–2 ms vs. 150–250 ms), 3–5× lacnejší round trip zmení hodnotu E2 a vtedy je zbytočná aj ona. Plán preto správne meria najprv, ale gate E1m musí mať default, keď merania nie sú dostupné (dnes: bez DB a bez runtime logov).
**Napätia:** 1. Trvanlivosť vs. počet round tripov (E1b-2.3, E2.2): checkpoint po každom efekte (`effects.ts:209–218`, `continuation.ts:110–136`) dáva presný kurzor pri páde uprostred – replay nikdy nezopakuje presence RPC ani UPDATE pokusu; jeden checkpoint po dávke ušetrí ≈ 12 round tripov, ale pri páde medzi dávkou a checkpointom sa dávka replayuje celá a porazený ostáva 1–2 s dlhšie „ringing“ (eligibilita iných hovorov číta `result = offered`, `session-runner.ts:311`). Idempotencia efektov (leg UPDATE, attempt UPDATE s `.in(result)`, presence s tokenom/revíziou) to robí bezpečným, no cenou je, že chyby v idempotencii sa prejavia až pri páde – preto je test „crash medzi dávkou a checkpointom“ nevyhnutný, nie voliteľný. 2. Ochrana zákazníckej vetvy vs. auto-bridge providera (B): `bridge` na zákazníckej vetve s `park_after_unbridge: self` (`transitions.ts:1216–1218`, komentár `:2081–2094`) je jediná vec, ktorá pri lazy conference promotion zachráni zákazníka; `bridge_on_answer` presunie spojenie do Telnyxu za 0 round tripov, ale guard first-answer-wins v stage RPC (`20260928110000:44–52`) by už len kompenzoval a hold/consult by sa museli prerobiť – experiment je správne mimo základ. 3. Pravdivé 200 vs. „2xx okamžite“ (N4, E1c): 500 pri neprevzatom lease (`event-processor.ts:371`) je jediný pravdivý signál, že fakt nebol aplikovaný, a Telnyx ho opakuje; „200 + after()“ by bez workera stratilo fakt pri páde invokácie. Cena: amplifikácia 2,5× na tom istom poole. Ohraničené čakanie webhooku (≤ 1,2 s) toto zmierni, ale súperí o lease s klikom operátora, ktorý má prioritu z PR #171 (`session-runner.ts:452–460`) – plán správne viaže E1c na nameraný p95 handlera < 5 s, inak sa čakanie iba presunie z Telnyxu do Vercelu.
**Syntéza:** Zachovať: E0 (allowlist → denylist + post-deploy gate 401), E1a ako etapu bez súbehu (lean kontext pod `noContinuation`, bridge-first s kompenzáciou bez `playback_stop`, deduplikácie a–f), E1m ako rozhodovací bod, preferenciu E2 pred E1b-2, nálezy N10/N11, Prílohu A (počty sedia krok za krokom). Zmeniť: (a) E2.1 dizajn fence – `motorist_telephony_fence` (`20260929200000:23–37`) nečíta `motorist.telephony_internal`, čítajú ho iba triggery (`:43`); admit RPC potrebuje interné (nefencované) varianty `lease_acquire`/`pending_commands`/`observe_dial` alebo výslovne testovaný lokálny skrat vo fence funkcii; (b) E1a.2(b) – `gather_stop` v kompenzácii len pre `waiting`/`parked` bez `meta.queue`, lebo pri fronte `enterWaiting` vráti skoro (`transitions.ts:1013–1017`) a gather fronty musí bežať ďalej; (c) E1a.1 – lean kontext len pre intenty `ring`/`pickup`/`transfer`/`transfer_safe` (nie `internal`, `transitions.ts:1096` číta `ctx.announcements`/`ctx.line`), a follow-continuation slučka (`session-runner.ts:642–656`) nesmie znovu použiť lean kontext; (d) E1a.3(d) – mapovanie PT409 iba pre správu „telephony termination blocks new provider command“ a iba pre ne-teardown príkazy, so zachovaným `checkpointCommand(key)`; (e) E1a.4 – cache kill-switchu z E1a vypustiť (−1 round trip nestojí za zmenu sémantiky ani za otázku používateľovi), riešiť v admit RPC; (f) opraviť `api-auth.ts:159–165` → `:167–171` (profil ide cez user-JWT klienta, v `steps.db` sa nepočíta) a cestu `routing/reservation.ts:48`; (g) E1m – default pri nedostupných meraniach: predložiť žiadosť o E2, nie blokovať. Ciele: E1a ≈ 26 je z kódu odvoditeľné a dosiahnuteľné; E1b-2 ≤ 16 je dosiahnuteľné v počte, nie preukázateľne vo wall-clock (N11); fanout „10 + 1 HTTP“ bez E2.3 nie je dosiahnuteľný (12–13 je strop); E2 ≤ 8 je dosiahnuteľné (5–6) iba s opraveným dizajnom fence a s admit RPC, ktorý vracia settings + legs + attempts.
**Faktické chyby (6):**
1. E2.1: `motorist_telephony_fence` (`supabase/migrations/20260929200000_fenced_telephony_commands.sql:23–37`) nekontroluje `motorist.telephony_internal`; skrat je len v `motorist_telephony_session_write_guard` (`:43`). Admit RPC, ktoré by volalo `motorist_provider_observe_dial_v2` (fence na `:274`) alebo `prepare_v2` (`:215`) pred tým, než klient pozná nový token/generáciu, skončí na PT409 – plánom navrhnutý `set_config` bypass nefunguje bez zmeny fence funkcie alebo interných variantov RPC.
2. E1a.2(b): tvrdenie „`enterWaiting` vydá nový `gather` (`:1022`)“ platí len pre `waiting`/`parked` bez fronty; pri `meta.queue` je `queued = true` a `previous = meta.waiting`, `enterWaiting` vráti na `transitions.ts:1017` bez `gather` a bez `startQueuePrompt` – `gather_stop` v kompenzácii by zastavil gather fronty bez náhrady.
3. N5, N8, E4: `src/server/api-auth.ts:159–165` je vetva `unauthenticated`; profil sa číta na `:167–171` cez `createSupabaseServerClient()` (user JWT), nie cez admin klienta s `telephonyDatabaseFetch` – nie je v `steps.db.count` (`request-metrics.ts:22–29`, `admin.ts:13–14`).
4. §2 r. 2, N2, E1b.4: `reservation.ts:48` neexistuje v `state/`; `authorizeOperatorDispatch` je `src/server/telephony/routing/reservation.ts:48–50` (obal nad `transitionPresence`, `:25–36`).
5. N2, E1b.3: `assertOwnership` v `executeDial` je `effects.ts:996` (nie `:997`); slučka attempt UPDATE je `:223–232` (nie `:229–237`); checkpoint po každom efekte je `:209–218` (nie `:211–222`). Nemenia počty.
6. E1a.1: `onInternalCalleeAnswered` číta `b.ctx.announcements ?? announcementConfigFromMetadata(b.ctx.line?.metadata)` (`transitions.ts:1096`); lean kontext s `line: null` a `announcements` len z meta mení rozhodnutie pre session bez `meta.announcements` – intent `internal` nie je v lean množine bezpečný bez ďalšej podmienky.
**Porušenia princípov (5):**
1. Princíp 3 (invarianty kontraktu 2, fence): E2.1 opisuje obídenie fence, ktoré kód nepodporuje; bez výslovného návrhu zmeny `motorist_telephony_fence` alebo interných variantov RPC by implementácia buď zlyhala, alebo by fence oslabila mimo rozsahu RPC.
2. Princíp 5 (žiadna požiadavka na nesprávnom čítaní kódu): E1a.2(b) `gather_stop` v kompenzácii pre pickup z fronty vychádza z nesprávneho čítania `enterWaiting` (`transitions.ts:1006–1017`).
3. Princíp 3 (STOP bariéry a nahrávacie rozhodnutia, lean len pod `noContinuation`): E1a.1 nechráni follow-continuation slučku (`session-runner.ts:642–656`), ktorá reduce spúšťa s `{ ...context, now }` – lean kontext by sa použil aj pre `sweep` follow s prázdnou presence/devices.
4. Princíp 4 (prevádzková zmena = samostatný súhlas) aplikovaný nadbytočne: E1a.4 žiada rozhodnutie používateľa o 1 s cache kill-switchu za −1 round trip; správne je zmenu vypustiť, nie ju eskalovať.
5. Princíp 2 (cieľové čísla odvodené z opravených počtov): §8 kritérium fanoutu „≤ 6 + N HTTP“ je nedosiahnuteľné bez E2.3 (parciálne unikátne indexy `20260903100000:360–371` bránia dávkovému upsertu cez PostgREST), no §8 ho neviaže na E2.
**Požadované zmeny (11):**
1. E2.1: nahradiť tvrdenie, že `set_config('motorist.telephony_internal','1',true)` obíde fence pre `observe_dial_v2`/`pending_commands_v2`/`lease_acquire_v2` volané vnútri admit RPC – `motorist_telephony_fence` (`20260929200000:23–37`) toto nastavenie nečíta, čítajú ho len triggery (`:43`); navrhnúť interné nefencované varianty alebo lokálny skrat vo fence funkcii s testom v `tests/postgres/telephony-fencing.py`, že mimo transakcie RPC neplatí.
2. E1a.2(b): obmedziť `gather_stop` v kompenzácii `bridgeId` na pickup z `WAITING_STATES` bez `meta.queue`; pri fronte `enterWaiting` vráti skoro cez `previous` (`transitions.ts:1013–1017`) bez nového `gather` a gather fronty musí bežať ďalej – doplniť test „pickup z fronty, bridge zlyhá → gather fronty ostáva“.
3. E1a.1: zúžiť lean kontext na intenty `ring`/`pickup`/`transfer`/`transfer_safe`; `internal` vylúčiť (alebo podmieniť prítomnosťou `meta.announcements`), lebo cesta callee číta `ctx.announcements`/`ctx.line?.metadata` (`transitions.ts:1096`).
4. E1a.1: výslovne určiť, že bounded follow-continuation slučka (`session-runner.ts:642–656`, follow `sweep`/`recording_continue`) pri lean ceste buď načíta plný kontext, alebo sa preskočí – inak `planRingStep` dostane prázdnu presence/devices; doplniť test s `gather.failed`.
5. E1a.3(d): spresniť mapovanie – iba `PT409` s textom „telephony termination blocks new provider command“ z `prepare_v2` (`20260929200000:227–230`), iba pre ne-teardown príkazy, mapované na existujúcu vetvu superseded vrátane `checkpointCommand(key)` (`effects.ts:1368–1371`); všetky ostatné PT409 ostávajú ako dnes (`ownership.ts:52`).
6. E1a.3(f): doplniť fallback SELECT identity zdrojovej vetvy pre `bridge`, keď `ctx` nemá snapshot legs (replay z cronu cez `resumePendingEffects`, `cron-jobs.ts:156`).
7. E1a.4: vypustiť cache live-gate settings z E1a (zmena sémantiky kill-switchu za −1 round trip) a presunúť ju do E2.1 admit RPC; ponechať len cache `resolveDefaultOrganizationId`.
8. N5, E4, §8: opraviť citáciu `api-auth.ts:159–165` na `:167–171` a uviesť, že profil sa číta serverovým (user-JWT) klientom mimo `telephonyDatabaseFetch`, takže nie je v `steps.db.count`; meranie podielu auth v E4 viazať na krok `auth` v `server-timing` (`call-action-route.ts:55`).
9. §2, N2, E1b.4, E3: opraviť cestu `reservation.ts:48` na `src/server/telephony/routing/reservation.ts:48` a posuny riadkov `effects.ts:997` → `:996`, `:229–237` → `:223–232`, `:211–222` → `:209–218`.
10. E1m: doplniť default rozhodnutia, keď merania (runtime logy, SQL A/D/I) nie sú v okne dostupné – predložiť používateľovi žiadosť o E2 s dnešným odhadom ceny round tripu, nie blokovať ďalšie etapy.
11. §5/§8: uviesť, že cieľ fanoutu „≤ 6 + N HTTP“ platí iba pre E2.3 a že bez migrácie je strop ≈ 12–13 sekvenčných round tripov wall-clock (N11), aby §8 neobsahoval kritérium, ktoré E1b-2 nemôže splniť.

## Recenzia Architekta – iterácia 4 (17. 9. 2026, snapshot `f8cb071`)

### Overenie kódu
Otvoril som všetky rozhodujúce odkazy v `/tmp/pm-main`: `transitions.ts` (136–138, 507–509, 852, 968–1025, 1106–1132, 1169–1280, 2078–2160, 2252–2420, 2665–2700), `effects.ts` (186–275, 300–345, 685–710, 990–1230, 1256–1816), `continuation.ts` (40–136), `session-runner.ts` (60–170, 200–370, 425–704), `event-processor.ts` (81–109, 187, 230–374, 435–488), `client.ts` (42–45, 469–543), `provider-journal.ts`, `ownership.ts`, migrácie `20260929200000`, `20260928110000`, `20260928100000`, `20260903100000`, `coordinated-webphone.ts`, `ring-plan.ts`, `call-actions.ts`, `runtime.ts`, `api-auth.ts`, `request-metrics.ts`, `recording.ts`, `participants.ts`, `termination.ts`, `cron/route.ts`, `vercel.json` a obe citované docs.

**Príloha A sedí krok za krokom.** Deps 2, claim 1, `findSession` 1, probe + acquire + vnorený `assertOwnership` 3 (`ownedSessionWork` sa volá dvakrát: `event-processor.ts:288` a `runSessionEvent:485` → `:437`), snapshot 1, evidence 2 (`call.answered` je v `ELIGIBLE`, `provider-event-evidence.ts:14`), kontext 3–4, stage + fresh 2, guard 0 (v stage RPC), kritické efekty 15 + 1 (leg patch a attempt patch sú deduplikované podľa kľúča, `transitions.ts:183–200`; presence porazených bez `afterCommandId`), príkazy 5/6/5/5 – `bridge` po ≈ 40, spolu ≈ 60–62 + 4 HTTP. N8 (14 pred prvým hangup HTTP) sedí: `dispatchUrgentTeardown` beží z `resumePendingEffects:1711` pred `persistTransition`. N10 potvrdené (`fork()` z pôvodného riadku, `mohIsPlaying(failed)` true, `enterWaiting` nereštartuje MOH). N11 potvrdené (`for update` v `motorist_telephony_fence:27`, `lease_acquire_v2:113`, `prepare_v2:216`, `result_v2:243`, `presence_transition_v1:83`). `effects.ts:1585` skutočne vyradí všetky príkazy záznamu. Dedupe (a) je bezpečná – acquire práve nastavil `lease_until = now + 15 s`, vnorený renew je redundantný; (b), (c), (e) sú bezpečné; (d) je bezpečná len s pomenovaným mapovaním PT409.

### Faktické chyby
1. **E2.1 dizajn fence nefunguje tak, ako je napísaný.** `motorist_telephony_fence` (`:23–37`) porovnáva iba hlavičky s `lease_token`/`ownership_generation`; `motorist.telephony_internal` číta len trigger (`:43`). Admit RPC, ktoré by vnútri volalo `observe_dial_v2` (fence `:274`) alebo `prepare_v2` (`:215`) skôr, než klient pozná nový token, dostane PT409. Treba interné nefencované varianty alebo lokálny, testovaný skrat vo fence funkcii.
2. **E1a.2(b) pre pickup z fronty je nesprávne.** Pri `meta.queue` `enterWaiting` vráti na `transitions.ts:1017` bez nového `gather`; `gather_stop` v kompenzácii by umlčal gather fronty. Platí len pre `waiting`/`parked` bez fronty.
3. `api-auth.ts:159–165` je vetva `unauthenticated`; profil je `:167–171` cez user-JWT klienta – nie je v `steps.db.count`.
4. `reservation.ts:48` je `routing/reservation.ts:48`; drobné posuny `effects.ts:996`, `:223–232`, `:209–218`.
5. `onInternalCalleeAnswered` číta `ctx.announcements`/`ctx.line?.metadata` (`transitions.ts:1096`) – intent `internal` nepatrí do lean množiny bez ďalšej podmienky.

### Antitéza
„E0 → E1a → rovno žiadosť o E2; E1m ako kontrola, nie gate; E1b vypustiť.“ E1a odstráni len duplicity (−14), mechanizmus ostáva; pri 150–250 ms je 26 round tripov 4–6,5 s, teda mimo 2 s – výsledok E1m je pri dnešnej cene predvídateľný. Súbeh v Node sa serializuje na zámku riadku session a otvára nové režimy zlyhania. Plán antitézu správne neprijíma úplne: bez súhlasu s migráciou je E1b-2 jediná cesta pod 26 a lacnejší round trip (gateway/pool) môže urobiť zbytočnou aj E2. Chýba však default pre prípad, že merania nebudú dostupné – vtedy sa má predložiť žiadosť o E2, nie blokovať.

### Napätia
- **Trvanlivosť vs. round tripy**: checkpoint po každom efekte dáva presný kurzor; jeden checkpoint po dávke ušetrí ≈ 12, ale bezpečnosť stojí na idempotencii, ktorá sa prejaví až pri páde – test „crash medzi dávkou a checkpointom“ je nutný.
- **`park_after_unbridge` vs. auto-bridge**: `bridge` na zákazníckej vetve (`:1216–1218`, `:2081–2094`) chráni zákazníka pri promotion; auto-bridge to stráca a guard by len kompenzoval – experiment mimo základ je správne.
- **Pravdivé 500 vs. „2xx okamžite“ a priorita ovládania**: 500 pri neprevzatom lease (`event-processor.ts:371`) je jediný pravdivý signál; ohraničené čakanie webhooku súperí s klikom operátora (`session-runner.ts:452–460`), preto je viazanie E1c na p95 < 5 s správne.

### Etapy a invarianty
E0: bez kódu, gate 401 na nemennom hoste sedí s `cron/route.ts:42–44`; O2 správne označené ako hypotéza. E1a: kompenzácia bridge-first nesie hangup víťaza a (opravené) `gather_stop` len mimo fronty; lean kontext musí vylúčiť `internal` a follow-continuation slučku (`session-runner.ts:642–656`), inak `planRingStep` dostane prázdnu presence. E1a.4 cache kill-switchu: vypustiť, nie eskalovať používateľovi. E1b-1: paralelný teardown je bezpečný (hangup bez bookkeepingu, `:688–699`); journalová skupina len pod `noContinuation` je správne. E1b-2: idempotencia dávky sedí; N jednoriadkových INSERTov je nutné kvôli parciálnym indexom. E1c: podmienky správne. E2: fence dizajn opraviť; claim/busy sémantika a mixed-writer matica sú správne. Žiadne porušenie obmedzení repozitára (jeden cron, bez workerov, migrácie na súhlas, iba táto kópia).

### Dosiahnuteľnosť cieľov
E1a ≈ 26 pred `bridge`: odvoditeľné a dosiahnuteľné (každá odstránená položka je reálny round trip). E1b-2 ≤ 16: dosiahnuteľné v počte, nie preukázateľne vo wall-clock (N11). Fanout „10 + 1 HTTP“ bez E2.3 nedosiahnuteľný – strop ≈ 12–13; §8 to musí viazať na E2. E2 ≤ 8: dosiahnuteľné (5–6) iba s opraveným dizajnom fence a admit RPC vracajúcim settings + legs + attempts.

### Verdikt
**REVISE** – tri vecné chyby (fence v E2.1, `gather_stop` pri fronte, rozsah lean kontextu) a niekoľko spresnení; jadro plánu, poradie etáp a Príloha A obstoja.

## critic:v3
**Verdikt: ITERATE**
**Výhrady (14):**
1. (m) Plán je revízia 3; 11 požadovaných zmien a 6 faktických opráv Architekta z iterácie 4 nie sú zapracované ani odmietnuté s dôvodom. Vyžaduje sa revízia 4 so záznamom v Prílohe B/C.
2. E2.1 dizajn fence je nesprávny: `motorist_telephony_fence` (`supabase/migrations/20260929200000_fenced_telephony_commands.sql:23–37`) porovnáva iba hlavičky s `lease_token`/`ownership_generation`; `set_config('motorist.telephony_internal','1',true)` číta len trigger `motorist_telephony_session_write_guard` (`:43`). `prepare_v2` (`:215`) a `observe_dial_v2` (`:274`) volajú fence priamo, takže admit RPC, ktoré v tej istej transakcii získa novú generáciu a potom ich zavolá, skončí PT409. Nahradiť: interné nefencované varianty, alebo transakčne lokálny skrat vo fence funkcii s testom v `tests/postgres/telephony-fencing.py`, že mimo transakcie RPC neplatí.
3. E1a.2(b): `gather_stop` v kompenzácii `bridgeId` obmedziť na pickup z `WAITING_STATES` bez `meta.queue`. Pri fronte je `queued = true`, `previous = meta.waiting` a `enterWaiting` vráti na `transitions.ts:1013–1017` bez nového `gather`; kompenzačný `gather_stop` by umlčal gather fronty bez náhrady. Doplniť test „pickup z fronty, bridge zlyhá → gather fronty beží ďalej“.
4. E1a.1 lean kontext: (a) citácia Architekta `transitions.ts:1096` je `canDirectConnectOutbound` (cesta `outbound`), nie `onInternalCalleeAnswered` (`:1069–1092`), ktorá z kontextu nečíta nič; intent `internal` napriek tomu z lean množiny vylúčiť alebo uviesť, že session s `direction = internal` nemá `meta.recording` (`recording.ts:32`), takže `noContinuation` nikdy neplatí. (b) Slučka follow-continuation (`session-runner.ts:642–656`) opakovane používa ten istý `context`; na lean ceste by `sweep` po `meta.gather.failed` dal `planRingStep` prázdnu presence/devices. Určiť: pred slučkou načítať plný kontext alebo slučku preskočiť; test s `gather.failed`.
5. E1a.3(d): obmedziť na sessions pod `noContinuation` (nahrávacia cesta má vlastné čerstvé čítanie `effects.ts:1377–1380` a `commandStillApplies` číta stav recorderov); mapovanie spresniť — iba PT409 so správou „telephony termination blocks new provider command“ z `prepare_v2` (`:227–230`) na existujúcu vetvu superseded vrátane `checkpointCommand(key)` (`effects.ts:1368–1371`); ostatné PT409 ostávajú ako dnes (`ownership.ts:52`).
6. E1a.3(f): identita zdrojovej vetvy pre `bridge` zo snapshotu `legs` potrebuje fallback SELECT, keď `ExecutionContext` snapshot nemá (replay z cronu cez `resumePendingEffects`, `effects.ts:1723`; `dispatchUrgentTeardown`, `:1690`).
7. E1a.4: cache live-gate settings vypustiť z E1a (zmena sémantiky kill-switchu za −1 round trip) a presunúť do E2.1 admit RPC (settings vrátené v tej istej transakcii); v E1a ponechať len cache `resolveDefaultOrganizationId`.
8. Citácie: `api-auth.ts:159–165` je vetva `unauthenticated`; profil sa číta na `:167–173` cez `createSupabaseServerClient()` (user JWT), nie cez `telephonyDatabaseFetch`, teda nie je v `steps.db.count` — N5/N8/E4/§8 upraviť a podiel auth merať krokom `auth` v `server-timing` (`call-action-route.ts:55`). `reservation.ts:48` → `src/server/telephony/routing/reservation.ts:48` (`state/reservation.ts` neexistuje). `effects.ts:997` → `:996`, `:229–237` → `:223–232`, `:211–222` → `:209–218`; guard `onConsultAnswered` je `:1272–1277`.
9. E1m: chýba default pre prípad, že merania (runtime logy, SQL A/D/I) nebudú v okne dostupné — táto relácia sama uvádza odmietnutý token pre runtime-logs API. Určiť ohraničené okno, po ktorom sa používateľovi predloží žiadosť o E2 s dnešným odhadom ceny round tripu (OpenAI 13. 9.), a že E1b-1 beží bez ohľadu na výsledok.
10. §5/§8: kritérium fanoutu „posledný člen ≤ 1 s po prvom“ je splniteľné len s E1b-2 alebo E2.3 (sériový dial ≈ 13 round tripov × 150–250 ms na člena); viazať ho na tieto etapy. Uviesť, že „≤ 6 + N HTTP“ platí iba pre E2.3 a strop bez migrácie je ≈ 12–13 sekvenčných round tripov wall-clock (N11).
11. E1b-1.2: „`completedCommands` sa doplní len pre úspešné príkazy“ je v rozpore s dnešnou sémantikou kompenzácie (`effects.ts:1585` po stage-ovaní nástupcu vyradí všetky príkazy záznamu, aby sa deterministické odmietnutie neopakovalo). Definovať per-výsledok spracovanie skupiny po `allSettled`: odmietnutý `bridge` s kompenzáciou vyradí celú skupinu ako dnes; zlyhanie bestEffort príkazu podľa existujúcej vetvy zlyhania; neznámy výsledok per príkaz.
12. E3 a E4 nemajú výslovné riadky Stop/Rollback (E0–E2 ich majú); doplniť aspoň stop-signály (presence neuvoľnená, duplicitná registrácia mobilu, nárast 401/5xx na pollingu) a rollback (revert vetvy).
13. Atribúcia: hodnota „0–0,42 s (outbound)“ v §1/N9 pochádza z auditu OpenAI z 11. 9. (`docs/operations/telephony-latency-audit-2026-09-11.md`); uviesť zdroj výslovne, ako pri 44–72 operáciách.
14. Test plán: doplniť reducer test pre nahrávanú session s bridge-first — `startBeforeAudio` (`recording.ts:161`) hľadá `bridge` podľa druhu a vkladá štart nahrávania pred neho; test dokáže tvrdenie „bez zmeny nahrávacích rozhodnutí“.
**Kontrola odkazov:** 42 overených, 6 s nálezom.
- transitions.ts:1096 ako onInternalCalleeAnswered (Architekt) — WRONG: je to canDirectConnectOutbound; onInternalCalleeAnswered je :1069–1092 a z ctx nečíta nič
- effects.ts:211–222 checkpoint po každom efekte — WRONG offset (presne :209–218)
- effects.ts:229–237 slučka attempt UPDATE — WRONG offset (presne :223–232)
- effects.ts:997 assertOwnership v executeDial — WRONG (presne :996)
- api-auth.ts:153–157 getUser (GoTrue) — OK; :159–165 profil — WRONG (vetva unauthenticated; profil :167–173 cez user-JWT klienta, mimo db.count)
- reservation.ts:48 — WRONG cesta (src/server/telephony/routing/reservation.ts:48; state/reservation.ts neexistuje)

## Recenzia Critica – iterácia 4 (17. 9. 2026, snapshot `f8cb071`, read-only)

### Čo obstojí
Jadro plánu je správne a dôkazmi podložené. Prílohu A som prešiel krok za krokom: deps 2, claim 1, `findSession` 1, probe + acquire + vnorený renew 3, snapshot 1, evidencia 2, kontext 3–4, stage + fresh 2, guard 0 (v `motorist_stage_transition_v1:44–52`), kritické efekty ≈ 16, príkazy 5/6/5/5 → `bridge` po ≈ 40, spolu ≈ 60–62 + 4 HTTP. N8 (14 pred prvým hangup HTTP), N10 (tichá čakáreň: `fork()` z pôvodného riadku, `mohIsPlaying(failed)` true, `enterWaiting` MOH nereštartuje) a N11 (`for update` vo fence, acquire, prepare, result, presence) sú potvrdené čítaním kódu. Lean kontext pod `noContinuation` je bezpečný: `onOfferAnswered` vrátane forku číta len `settings.parkMaxMinutes`, `mediaAvailable`, `now`, `recordingPolicy`; zmrazená vypnutá politika nemôže byť živou politikou znovu zapnutá (`eligible` vyžaduje obe). Princípy sú konzistentné s voľbami, alternatívy A–F sú férové s výslovnou invalidáciou D, pre-mortem má presne tri scenáre s prevenciou a overením, kritériá §8 majú vzorky, test plán pokrýva unit/integráciu/e2e/observabilitu, obmedzenia repozitára (jeden cron, bez workerov, migrácie len na súhlas, iba táto kópia) sú dodržané, hypotézy sú označené a čísla OpenAI atribuované, self-review iterácie 1 je priznaný. Plán odpovedá na zadanie: prijatie/bridge (E1a, E1b, E2), pridanie účastníka a presmerovanie (E3 vrátane reduceru a životného cyklu presence), hangup (N8, E1b-1), nahrávanie mimo priority s ponechanými ochranami.

### Prečo ITERATE
1. **Formálne**: plán je revízia 3 a nezapracúva 11 požadovaných zmien ani 6 faktických opráv Architekta z iterácie 4. Kritérium (m) nie je splnené.
2. **E2.1 fence** (potvrdené): `motorist_telephony_fence` (`20260929200000:23–37`) porovnáva iba hlavičky; `motorist.telephony_internal` číta len trigger (`:43`). `prepare_v2` (`:215`) a `observe_dial_v2` (`:274`) fence volajú priamo, takže admit RPC s novou generáciou v tej istej transakcii skončí PT409. Navrhnutý `set_config` bypass nefunguje bez interných variantov alebo transakčne lokálneho skratu vo fence s testom.
3. **E1a.2(b) pri fronte** (potvrdené): `enterWaiting` s `meta.queue` vráti na `transitions.ts:1013–1017` bez nového `gather`; kompenzačný `gather_stop` by umlčal gather fronty. Obmedziť na `WAITING_STATES` bez fronty.
4. **Follow slučka** (`session-runner.ts:642–656`) opakovane používa ten istý `context`; na lean ceste by `sweep` dostal prázdnu presence/devices. Treba plný kontext alebo preskočenie.
5. **Korekcia Architekta**: `transitions.ts:1096` je `canDirectConnectOutbound` (outbound), nie `onInternalCalleeAnswered` (`:1069–1092`, z ctx nečíta nič). Vylúčenie `internal` je napriek tomu lacné a obhájiteľné (`direction = internal` nemá `meta.recording`, `recording.ts:32`).
6. **Presnosť**: `api-auth.ts:159–165` je vetva `unauthenticated`, profil ide na `:167–173` cez user-JWT klienta mimo `steps.db.count`; `reservation.ts:48` je v `routing/`; posuny `effects.ts:996`, `:223–232`, `:209–218`.
7. **Rozhodovanie**: E1m nemá default pri nedostupných meraniach; §8 fanout „≤ 1 s“ je splniteľné len s E1b-2/E2.3; E1a.4 cache kill-switchu vypustiť a presunúť do E2.1 admit; E1b-1.2 „`completedCommands` len pre úspešné“ koliduje s `effects.ts:1585`.
8. **Menšie**: E3/E4 bez Stop/Rollback; „0–0,42 s“ atribuovať auditu OpenAI z 11. 9.; doplniť reducer test, že `startBeforeAudio` (`recording.ts:161`) pri bridge-first stále vkladá štart nahrávania pred `bridge`.

### Verdikt
**ITERATE** – žiadna chyba nie je zásadná pre smer plánu; tri vecné (fence v E2.1, `gather_stop` pri fronte, follow slučka pod lean kontextom) a súbor spresnení sú opraviteľné v revízii 4 bez zmeny poradia etáp E0 → E1a → E1m → E1b-1 → (E2 | E1b-2) → E1c → E3 → E4.

## planner:v4
Revidovaný plán: 13850 slov.
**Zapracované zmeny:**
- E2.1 dizajn fence prepracovaný: overené, že `motorist_telephony_fence` (`20260929200000:23–37`) číta len hlavičky a `motorist.telephony_internal` číta iba trigger `:43`; fence volajú priamo `prepare_v2` `:215`, `observe_dial_v2` `:274`, `pending_commands_v2` `:294`; `lease_acquire_v2` vyžaduje len `x-telephony-writer` (`:116–118`); navrhnuté O-A (interné `_impl` varianty bez grantu pre `service_role`, vzor `:335–345`), O-B (transakčne lokálny skrat s testom mimo transakcie), O-C (bez zmeny fence, +1 round trip); počet 9 → 2/3 vrátane live gate; nový nález N12; invarianty a testy v `tests/postgres` rozšírené.
- E1a.2(b): `gather_stop` v kompenzácii `bridgeId` len pre `WAITING_STATES` bez `meta.queue`; pri fronte `enterWaiting` vráti skoro (`transitions.ts:1007–1008, 1015–1019`) bez nového `gather`; N10 rozšírený o variant fronty (dnešné poradie zastaví gather fronty; obnova zvuku fronty označená ako hypotéza); nové testy a gate „pickup z fronty ×3“.
- E1a.1: intent `internal` vylúčený z lean množiny s overeným dôvodom (`transitions.ts:706` → `onInternalCalleeAnswered` `:1069–1092`, ktorá z ctx nečíta nič; `:1096` je `canDirectConnectOutbound`; interné sessions nemajú `meta.recording`, `recording.ts:32, 203–204`); follow-continuation slučka (`session-runner.ts:642–656`, `:651`) pri lean kontexte načíta plný kontext (príznak `lean`), test s `gather.failed`; doplnený predpoklad na overenie dátami, že produkčné sessions majú zmrazenú vypnutú politiku v `meta.recording` (inak `noContinuation` nikdy neplatí).
- E1a.3(d): zúžené na sessions pod `noContinuation`, iba PT409 so správou „telephony termination blocks new provider command“ z `prepare_v2` (`:227–230`), iba ne-teardown príkazy, mapované na vetvu superseded vrátane `checkpointCommand(key)` (`effects.ts:1368–1371`); ostatné PT409 nezmenené (`ownership.ts:52`).
- E1a.3(f): fallback SELECT identity zdrojovej vetvy pre `bridge`, keď `ExecutionContext` (`effects.ts:613–619`) nemá snapshot legs (`dispatchUrgentTeardown` `:1690`, replay z cronu `:1723`, `cron-jobs.ts:156`).
- E1a.4: cache live-gate settings vypustená z E1a (zmena sémantiky kill-switchu za −1 round trip), settings sa vracajú v admit RPC E2.1 v tej istej transakcii; ponechaná len cache `resolveDefaultOrganizationId`; princíp 4 doplnený.
- Citácie opravené po overení: `api-auth.ts:159–165` → `:167–173` (profil cez user-JWT klienta, mimo `telephonyDatabaseFetch`/`steps.db.count`; meranie krokom `auth` `call-action-route.ts:55`, `calls/active/route.ts:57`) v N5/N8/E4/E5/§8; `reservation.ts:48` → `src/server/telephony/routing/reservation.ts:48–50`; slučka pokusov `effects.ts:229–237` → `:225–232`; `applyPresenceChange` `:308–336`; guard `onConsultAnswered` `:1271–1277` (`:1275`); `startBeforeAudio` bridge `recording.ts:158`.
- Odmietnuté korekcie riadkov po overení v snapshote (zdokumentované v Prílohe C): `effects.ts:997` → `:996` (996 je signatúra, 997 je `await assertOwnership()`); `:211–222` → `:209–218` (209 je prázdny riadok, closure `effect` je 211–222, checkpoint 218–220); `:223–232` (223 je leg patch); `:1096` ako `onInternalCalleeAnswered`.
- E1m: ohraničené okno (5 pracovných dní od nasadenia E1a na dev alias alebo 30 vzoriek) a default — bez meraní sa predloží žiadosť o E2 s odhadom 150–250 ms (3,9–6,5 s pre 26 round tripov), E1b-1 beží bez ohľadu na výsledok; žiadosť o E2 sa predkladá už pri odovzdaní E1a (alt. G).
- §5/§8: kritérium fanoutu „posledný člen ≤ 1 s“ viazané na E1b-2/E2.3; „≤ 6 + N HTTP“ iba pre E2.3; strop bez migrácie ≈ 12–13 sekvenčných round tripov wall-clock (N11); princíp 2 doplnený.
- E1b-1.2: per-výsledkové spracovanie skupiny po `Promise.allSettled` (i)–(v) v súlade s dnešnými vetvami `effects.ts:1481–1592` — odmietnutý príkaz s kompenzáciou vyradí celý záznam (`:1585`), bestEffort zlyhanie podľa `:1536–1540`, neznámy výsledok per príkaz (`:1541–1552`), lease loss (`:1482`); jeden checkpoint pre úspešné kľúče namiesto `checkpointCommand` per príkaz (`:1352–1357`).
- E3 a E4: doplnené výslovné Stop a Rollback riadky.
- Atribúcia: 0–0,42 s uvedené so zdrojom `docs/operations/telephony-latency-audit-2026-09-11.md:64, 119` (n = 6, medián 0,18 s, p95 0,37 s, max 0,42 s) v §1 a N9.
- Test plán: reducer test nahrávanej session s bridge-first (`startBeforeAudio` vloží štart nahrávania pred `bridge`, `recording.ts:158`) ako dôkaz „bez zmeny nahrávacích rozhodnutí“; testy `_impl` cez PostgREST → 42501 a staré hlavičky po admit → PT409.
- §4: alternatíva G (antitéza it. 4, prijatá čiastočne), princípy 2/3/4/5 preformulované; §6 ostávajú tri scenáre (fence pridaný do scenára 1, follow slučka do scenára 3); §9–§11 zosúladené; hlavička na revíziu 4 s poznámkou o nezávislých agentoch; Príloha B doplnená o záznam iterácie 4; Príloha C doplnená o tabuľku iterácie 4 vrátane odmietnutých položiek s dôvodmi.
**Odmietnuté po overení:**
- Architect/Critic: `effects.ts:997` → `:996` — odmietnuté, lebo `:996` je signatúra `executeDial` a `await assertOwnership()` je na `:997` (overené `sed -n '996,998{=;p}'`).
- Architect/Critic: checkpoint po každom efekte `effects.ts:211–222` → `:209–218` — odmietnuté, lebo `:209` je prázdny riadok a closure `effect` s checkpointom je `:211–222` (checkpoint `:218–220`).
- Architect/Critic: slučka attempt UPDATE `:229–237` → `:223–232` — prijaté len čiastočne, lebo `:223` je `applyLegPatch`; správny rozsah slučky pokusov je `:225–232`, ktorý plán teraz uvádza.
- Architect: dôvod vylúčenia intentu `internal` cez `transitions.ts:1096` (`onInternalCalleeAnswered` číta `ctx.announcements`/`ctx.line`) — dôvod odmietnutý, lebo `:1096` je `canDirectConnectOutbound` a `onInternalCalleeAnswered` (`:1069–1092`) z ctx nečíta nič; samotné vylúčenie je zapracované s overeným dôvodom (`:706` vetvenie, `recording.ts:32, 203–204`).
- Architect: „E1b-1 aj E1b-2 vypustiť“ — odmietnuté pre E1b-1, lebo paralelný teardown a journalová skupina nezávisia od migrácií, nemenia počet round tripov a sú nízkorizikové; E1b-2 ostáva výlučne ako záloha pre prípad neschválenia E2.
- Architect: „fanout 10 + 1 HTTP bez E2.3 nie je dosiahnuteľný“ — formulácia „10 + 1 HTTP“ v pláne nie je (cieľ E1b.4 bol 12–13); §8 je napriek tomu explicitne viazaný na E1b-2/E2.3 a strop bez migrácie je uvedený.

## architect:v4
**Verdikt: REVISE**
**Antitéza:** Najsilnejšia alternatíva k jadru plánu: „E0 → E1a rozšírená o sekvenčný jediný checkpoint kritickej fázy → rovno E2; E1b-1 aj E1b-2 vypustiť.“ Plán zlučuje dve nezávislé veci — zrušenie checkpointu po každom efekte a paralelizáciu — a preto jediný checkpoint odkladá do E1b-2 „len bez E2“. Kód to nevyžaduje: `persistTransition` (`effects.ts:211–222`) zapisuje checkpoint po každom efekte len preto, že `input.continuation` existuje; sekvenčná kritická dávka (leg SELECT+UPDATE, 3 attempt UPDATE s `.is(ended_at,null)`, 2 presence RPC s tokenom/`onlyIfStatus`) je idempotentná presne tým argumentom, ktorý plán sám prijíma v E1b-2.3, a jeden `checkpointEffects` po dávke odstráni 6 sekvenčných round tripov (15 → 9) bez akéhokoľvek súbehu v Node, bez N spojení poolu a bez rizika N11. Tým by E1a dosiahla `bridge` po ≈ 20, nie 26, a E1b-2 by stratila hlavný dôvod existencie. Druhý pilier antitézy: `resolveSessionRecordingPolicy` (`recording-policy-service.ts:58–73`) vždy vráti politiku (pri vypnutých env prepínačoch alebo chýbajúcom riadku `disabled`), takže `meta.recording.policy.enabled === false` sa zmrazí na prvom reduce každej neinternej session — predikát `noContinuation` teda pokrýva prakticky celú produkčnú cestu a lean/dedupe vetvy nie sú okrajové, ale hlavné; ich hodnota je vyššia, než plán predpokladá, a „overenie dátami“ pred E1a.1 je zbytočné. Zvyšok mechanizmu (5–6 DB na príkaz, admisia 9) odstráni až E2, preto žiadosť o migrácie má ísť hneď s E1a a E1b-1 (zmena `prepareProviderRequest`, per-výsledkové spracovanie skupiny) je zložitosť navyše za prekrytie siete, ktoré nikto nezmeral.
**Napätia:** 1. Trvanlivosť (presný kurzor pri páde) vs. počet round tripov: checkpoint po každom efekte (`effects.ts:218–220`, `continuation.ts:106–136`) dáva pri páde presný `databaseCursor` a porazený operátor nikdy neostane „ringing“ dlhšie, než trvá jeden efekt; jediný checkpoint po kritickej dávke ušetrí 6 round tripov, ale pri páde uprostred replay opakuje celú dávku a porazený môže byť 1–2 s dlhšie obsadený — obhájiteľné, lebo každý efekt dávky je idempotentný a presence RPC s `onlyIfStatus: ["ringing"]` je pri replayi no-op; plán tento kompromis prijíma v E1b-2.3, no nedôsledne ho odmieta pre E1a. 2. Ochrana zákazníckej vetvy (`park_after_unbridge: self` na `bridge` vydanom aplikáciou, `transitions.ts:1218`, komentár `:2081–2094`) vs. Telnyx auto-bridge (`bridge_on_answer`+`prevent_double_bridge`): auto-bridge by dal 0 round tripov medzi answer a bridge, ale guard first-answer-wins v `motorist_stage_transition_v1` (`20260928110000:44–52`) by už len kompenzoval a hold/consult (`promoteToConference`) by stratili oporu — správne ostáva experiment. 3. Pravdivé 200/500 (`event-processor.ts:371` vracia 500 pri neprevzatom lease) vs. Telnyx „Return 2xx immediately“ a `webhook_timeout_secs` 10: okamžité 2xx by vyžadovalo worker (zakázané), pravdivé 500 amplifikuje doručenia; E1c (ohraničené čakanie) je kompromis, ktorý ale súperí s prioritou ovládania z PR #171 (`session-runner.ts:452–460`) — poradie „až po zrýchlení handlera“ je správne. 4. Súbeh v Node vs. serializácia na `FOR UPDATE` riadku session (`20260929200000:27, 113, 216, 245`): paralelná skupina/dávka prekrýva iba sieťovú latenciu za cenu spojení poolu popri pollingu (N5); to je dôvod, prečo sekvenčný jediný checkpoint (bez súbehu) dominuje nad paralelnou dávkou a prečo E2 (menej round tripov) dominuje nad E1b.
**Syntéza:** Zachovať: E0 (denylist + post-deploy gate), poradie E1a → žiadosť o E2 pri odovzdaní E1a, lean kontext pod `noContinuation` s príznakom a plným kontextom pre follow slučku, bridge-first s kompenzáciou bez `playback_stop` a `gather_stop` len mimo fronty (N10 overený v kóde: `fork()` `:136–138`, `meta` getter `:140–142` zlučuje pôvodný riadok s `metaPatch` buildera, takže fork nemá `waiting: null`/`queue: null` z `:1207`), deduplikácie a–f so zúženým mapovaním PT409, E1m s defaultom, N12 dizajn fence O-A/O-B/O-C, E1c po E1a. Zmeniť: (1) presunúť sekvenčný jediný checkpoint kritickej fázy z E1b-2.3 do E1a ako samostatný bod bez súbehu (cieľ E1a ≈ 20 pred `bridge`), paralelnú dávku ponechať výlučne ako E1b-2; (2) opraviť N3/E1a.1 — `policyState` nevracia undefined pri chýbajúcom riadku politiky, lebo `resolveSessionRecordingPolicy` vždy vráti politiku; predpoklad „overiť dátami“ nahradiť meraním pokrytia lean vetvy; (3) doplniť do Prílohy A `lease_release_v2` (`session-runner.ts:467–471`) a throttled recover (`effects.ts:1677`), súčet ≈ 61–63; (4) v E1b-1.2 pomenovať, že replay po páde medzi úspešným HTTP a jediným checkpointom sa opiera o `prepare_v2` s cached `accepted` (`20260929200000:221–225`, `client.ts:488–492`) a pridať test „0 provider HTTP pri replayi“; (5) v E2 uviesť predpoklad, že `createTelephonyDeps` konštruuje Telnyx klienta/live gate až po admit RPC, inak settings ostanú samostatným round tripom; (6) opraviť citáciu `result_v2` FOR UPDATE na `:245`.
**Faktické chyby (3):**
1. N3 „Dosah predikátu“ a E1a.1: tvrdenie „ak organizácia nemá riadok recording politiky, `policyState` vráti undefined (`recording.ts:31–32`), `meta.recording` sa nikdy nezapíše a `noContinuation` nikdy neplatí“ je nesprávne — `resolveSessionRecordingPolicy` (`src/server/telephony/recording-policy-service.ts:58–73`) vždy vráti objekt politiky (pri chýbajúcom riadku, chybe alebo vypnutých env prepínačoch `{ enabled: false, reason: "recording_disabled" | "recording_policy_unavailable" }`), takže `context.recordingPolicy` je na plnej ceste vždy definovaná a `policyState` vráti undefined iba pre `direction = internal`; zmrazený stav sa zapíše na prvom reduce každej neinternej session (`recording.ts:203–204`).
2. Príloha A: chýba `motorist_session_lease_release_v2` volaný vo `finally` `ownedSessionWork` (`session-runner.ts:467–471`) pred ledger finish (`event-processor.ts:332`) a pred HTTP odpoveďou (+1 sekvenčný round trip) a throttled `recoverTelephonyIncidentThrottled` (`effects.ts:1677`, 0–1); správny súčet je ≈ 61–63 sekvenčných, nie 60–62.
3. N11/E2.1: `motorist_provider_command_result_v2` berie `perform 1 … for update` na `20260929200000:245`, nie `:243` (`:243` je `begin`).
**Porušenia princípov (4):**
1. Princíp 5 (žiadna požiadavka sa neopiera o nesprávne čítanie kódu): predpoklad E1a.1 „overiť dátami, či `meta.recording` existuje“ a jeho dôvod (`policyState` undefined bez riadku politiky) stoja na nesprávnom čítaní `resolveSessionRecordingPolicy`.
2. Princíp 2 (počty odvodené z opravených počtov): Príloha A vynecháva lease release RPC a throttled recover; §8 základ ≈ 70 požiadaviek a súčet 60–62 nie sú úplné.
3. Princíp 1/2 (optimalizuje sa počet sekvenčných round tripov; hlasový príkaz pred evidenciou): plán viaže jediný checkpoint kritickej fázy (−6 round tripov bez súbehu) na E1b-2 „len bez E2“, hoci sekvenčná verzia nemení invarianty, neotvára N11 ani N5 a patrí do E1a.
4. Princíp 3 (každý nový súbeh je pomenovaný a testovaný): E1b-1.2 neuvádza výslovne, že bezpečnosť replayu po páde medzi úspešným HTTP skupiny a jediným checkpointom závisí od cached `accepted` v `prepare_v2`, a chýba test „replay vydá 0 provider HTTP“.
**Požadované zmeny (6):**
1. E1a (nový bod 6) a E1b-2.3: presunúť „jeden checkpoint po kritickej dávke“ do E1a ako sekvenčnú zmenu bez súbehu (leg/attempt/presence efekty, `createCallbackRequest` s vlastným checkpointom), s povinným crash-replay testom, cieľ E1a pred `bridge` ≈ 20; v E1b-2 ponechať iba paralelizáciu dávky.
2. N3 „Dosah predikátu“ a E1a.1 „Predpoklad na overenie dátami“: opraviť tvrdenie — `resolveSessionRecordingPolicy` (`src/server/telephony/recording-policy-service.ts:58–73`) vždy vráti politiku (pri vypnutých env prepínačoch alebo chýbajúcom riadku `disabled`), takže `context.recordingPolicy` je na plnej ceste vždy definovaná a `policyState` (`recording.ts:28–32`) vráti undefined len pre `direction = internal`; `meta.recording` sa zmrazí na prvom reduce každej neinternej session; predpoklad nahradiť read-only meraním podielu sessions s `policy.enabled === false` a bez pending entries.
3. Príloha A: doplniť krok 15b `motorist_session_lease_release_v2` v `finally` `ownedSessionWork` (`session-runner.ts:467–471`, pred ledger finish a odpoveďou) a `recoverTelephonyIncidentThrottled` (`effects.ts:1677`, 0–1); súčet uviesť ako ≈ 61–63 sekvenčných; §8 základ `steps.db.count` zosúladiť.
4. E1b-1.2 a §7: doplniť invariant a test „pád po `Promise.allSettled` pred jediným checkpointom → replay vydá 0 provider HTTP“, s odkazom na `prepare_v2` cached accepted (`20260929200000:221–225`) a `client.ts:488–492` (`decision.dispatch === false` → cached result).
5. E2 cieľ ≤ 8: uviesť predpoklad, že `createTelephonyDeps` (`runtime.ts:60–77`) musí konštruovať Telnyx klienta a live gate až po admit RPC (settings prichádzajú z admit), inak čítanie settings ostáva samostatným round tripom pred admit a cieľ 5–6 neplatí.
6. N11/E2.1 citácie: `motorist_provider_command_result_v2` berie `for update` na `20260929200000:245` (nie `:243`, to je `begin`).

## Posudok Architekta — revízia 4 plánu opravy odozvy hovorov (iterácia 5)

**Verdikt: REVISE** (6 požadovaných zmien, z toho 1 faktická chyba v mechanizme, 1 štrukturálna zmena poradia, 4 spresnenia).

### Overenie kódu (snapshot `f8cb071`)

Otvoril som všetky kľúčové odkazy: `transitions.ts:136–142, 202–210, 275–277, 415–420, 507–509, 706, 852, 968–1022, 1069–1092, 1095–1128, 1169–1257, 1271–1277, 2081–2151, 2255–2268, 2318–2346, 2385–2417, 2668–2675`; `effects.ts:140–143, 186–272, 308–336, 613–619, 688–706, 996–1101, 1133–1228, 1266, 1326–1357, 1358–1480, 1481–1592, 1628–1668, 1686–1723, 1779, 1800–1816`; `session-runner.ts:151–162, 205–368, 433–474, 477–480, 494–506, 538–543, 574–578, 642–656`; `event-processor.ts:81–108, 187, 236–288, 332, 371, 440–484`; `client.ts:42–43, 469–503`; `provider-journal.ts:31–36`; `continuation.ts:50–59, 78, 91–136`; `recording.ts:28–66, 122–125, 158, 203–204`; `ownership.ts:7–8, 34–38, 52, 58–65`; migrácie `20260929200000:23–37, 43, 92–107, 109–140, 211–238, 245, 274, 294, 304, 312, 323, 335–345`, `20260928110000:17–66`, `20260928100000:83`, `20260903100000:360–371`; `coordinated-webphone.ts:9, 272–311`; `ring-plan.ts:264–266`; `call-actions.ts:202–216, 255–288, 899–916`; `api-auth.ts:83, 153–173`; `runtime.ts:60–77`; cron, polling, PhoneBar, useTelephonyConsole a dokumenty. Všetky citácie existujú a s výnimkou dvoch bodov nižšie hovoria to, čo plán tvrdí. Príloha A je krok po kroku podložená: krok 4 = 3 (probe `:442`, acquire `:452`, vnorený renew `:436` cez `runSessionEvent`), krok 11 = 16 (jeden leg patch víťaza — `hangupCmd` `:342` legy nepatchuje, 3 attempt UPDATE, 2 presence, SELECT vetiev `:1339`), krok 12 = 5 na príkaz + 1 na `bridge`, `bridge` HTTP po ≈ 40. Stage RPC odstraňuje session patch z uloženého záznamu (`continuation.ts:78–83`), takže `persistTransition` v kontinuácii session UPDATE nevykoná — počet 15 kritických efektov sedí.

### Faktické chyby

1. **N3 / E1a.1 — mechanizmus predpokladu je nesprávny.** Plán tvrdí, že bez riadku recording politiky `policyState` vráti undefined a `meta.recording` sa nikdy nezapíše. `resolveSessionRecordingPolicy` (`recording-policy-service.ts:58–73`) však **vždy** vráti politiku — pri vypnutých env prepínačoch alebo chýbajúcom/nečitateľnom riadku vráti `disabled`. `context.recordingPolicy` je preto na plnej ceste vždy definovaná (aj pri `initiationOnly`, `session-runner.ts:238–253`) a `policyState` (`recording.ts:28–32`) vráti undefined len pre `internal`. Zmrazená vypnutá politika sa zapíše na prvom reduce každej inbound/outbound session (`recording.ts:203–204`). Dôsledok je pre plán priaznivý — `noContinuation` pokrýva prakticky celú hlavnú cestu — ale požiadavka „overiť dátami pred implementáciou“ stojí na nesprávnom čítaní (porušenie princípu 5).
2. **Príloha A — chýbajúci round trip.** `ownedSessionWork` v `finally` volá `motorist_session_lease_release_v2` (`session-runner.ts:467–471`) pred ledger finish a pred odpoveďou; plus throttled `recoverTelephonyIncidentThrottled` (`effects.ts:1677`, 0–1). Súčet je ≈ 61–63, nie 60–62. „Bridge po ≈ 40“ to nemení.
3. Drobnosť: `result_v2` berie `for update` na `20260929200000:245`, nie `:243`.

### Invarianty po etapách

- **E1a**: bridge-first neporušuje `startBeforeAudio` (hľadá `bridge` podľa druhu, `recording.ts:158`); kompenzácia z pôvodného riadku je potvrdená (`fork()` `:136–138`; `meta` getter `:140–142` zlučuje pôvodný riadok len s `metaPatch` daného buildera, takže fork nevidí `waiting: null`/`queue: null` z `:1207`). Mapovanie PT409 z `prepare_v2` (`:227–230`, správa mimo regexu `ownership.ts:52`) je nutné, inak by (d) menilo režim zlyhania na incident + `break` (`effects.ts:1549–1552`). Skip vnoreného renew hneď po acquire je bezpečný (TTL 15 s, `ownership.ts:8`).
- **E1b-1.2**: paralelní členovia nemenia `version` (prepare/result/renew berú zámok, ale verziu nemenia), takže jediný CAS checkpoint po `allSettled` nevyvolá `SessionConflictError`; replay po páde medzi úspešným HTTP a checkpointom sa opiera o cached `accepted` z `prepare_v2` (`:221–225`, `client.ts:488–492`) — plán to má výslovne pomenovať a testovať.
- **E1b-2.3**: poradie zámkov (leg/attempt riadok → session; presence RPC session → presence) je acyklické; lock-wait test je primeraný.
- **E2.1**: N12 potvrdený — fence porovnáva len hlavičky (`:29–35`), `telephony_internal` číta iba trigger (`:43`); O-A/O-B/O-C sú korektné. Cieľ ≤ 8 platí len ak sa Telnyx klient a live gate konštruujú až po admit (`runtime.ts:69–74`).
- Hypotézy (MOH pri bridge, nový gather pri gather, obnova zvuku fronty, cron user-agent, `getClaims`, cena round tripu) sú označené ako hypotézy; Telnyx tvrdenia zodpovedajú faktom kontextu. Obmedzenia repozitára sú dodržané (schválenia WAF/migrácie/Telnyx portál, jeden cron, žiadny worker, žiadny odkaz na pôvodný projekt).

### Dosiahnuteľnosť cieľov

E1a ≈ 26 pred `bridge` je odvodené konzistentne a dosiahnuteľné. **Chýba však lacná zmena bez súbehu**: sekvenčný jediný checkpoint po kritickej dávke (−6, 15 → 9) stojí na tom istom argumente idempotencie, ktorý plán prijíma v E1b-2.3, a neotvára N11 ani N5 — patrí do E1a a dá `bridge` po ≈ 20. E1b-2 ≤ 16 a fanout ≈ 12–13 wall-clock sú vierohodné (dávka = max(2,1,2) kôl + checkpoint). E2 ≈ 5–6 je dosiahnuteľné s uvedeným predpokladom o `createTelephonyDeps`.

### Napätia a syntéza

Pozri polia `tensions` a `synthesis`: zachovať E0, E1a, E1m s defaultom, N12 dizajn, E1c po zrýchlení handlera; presunúť sekvenčný jediný checkpoint do E1a; opraviť N3/E1a.1; doplniť Prílohu A, test replayu skupiny a predpoklad E2.

## critic:v4
**Verdikt: ITERATE**
**Výhrady (6):**
1. (m) Plán vo verzii 4 nezapracúva ani neodmieta 6 požadovaných zmien Architekta z iterácie 5; tri z nich sú overené faktické chyby v texte plánu: (1) N3/E1a.1 mechanizmus predpokladu je nesprávny — `src/server/telephony/recording-policy-service.ts:58–73` vždy vráti objekt politiky (`disabled` pri vypnutých env prepínačoch, chýbajúcom riadku alebo chybe) a plná cesta ho vždy načíta (`session-runner.ts:238–253`), takže `context.recordingPolicy` je definovaná a `policyState` (`recording.ts:28–32`) vráti undefined len pre `direction = internal`; navyše `recording.ts:33–39` zmrazí `enabled: false` (`start_announcements_disabled`) aj pri zapnutej živej politike bez štartovacích hlášok — predikát `noContinuation` teda pokrýva prakticky každú neinternú session; „overiť dátami pred implementáciou“ nahradiť read-only meraním podielu sessions s `metadata->'recording'->'policy'->>'enabled' = 'false'` bez pending entries a doplniť ho ako dotaz K do súboru meraní; (2) Príloha A vynecháva `motorist_session_lease_release_v2` vo `finally` `ownedSessionWork` (`session-runner.ts:469–471`, pred ledger finish `event-processor.ts:332` a odpoveďou) a throttled `recoverTelephonyIncidentThrottled` (`effects.ts:1679`, 0–1) — súčet ≈ 61–63 sekvenčných, §8 základ `steps.db.count` ≈ 71 a ciele ≤ 57/≤ 51/≤ 17 prepočítať (`db_count_at_dispatch` sa nemení, release je až po bridge); (3) N11/E2.1: `motorist_provider_command_result_v2` berie `for update` na `20260929200000:245`, nie `:243` (`begin`).
2. (a)/(m) Požiadavka Architekta 1 (sekvenčný jediný checkpoint po kritickej dávke do E1a) je vecne správna a bez súbehu: `applyLegPatch` (`effects.ts:263–272`), attempt UPDATE s podmienkami (`:227–229`) a presence RPC s tokenom/`onlyIfStatus` (`:323–336`) sú idempotentné, takže argument z E1b-2.3 platí aj sekvenčne a neotvára N11/N5. Aritmetika Architekta je však o 1 vedľa: 6 checkpointov → 1 ušetrí 5, teda 15 → 10 a `bridge` po ≈ 21 (nie ≈ 20). Plán má bod prevziať s povinným crash-replay testom (pád medzi dávkou a checkpointom → replay idempotentný, starší writer pokračuje od kurzora za dávkou) a `createCallbackRequest` ponechať s vlastným checkpointom.
3. (m)/(i) E2 cieľ ≤ 8 (≈ 5–6 pri O-A/O-B) platí len ak sa `createTelephonyDeps` (`src/server/telephony/runtime.ts:60–77`) prestavia tak, aby Telnyx klient a live gate vznikli až po admit RPC — dnes číta `motorist_telephony_settings` (`:69–74`) pred akoukoľvek prácou so session; bez tejto prerekvizity ostáva samostatný round trip pred admit a „settings v admit“ nič neušetrí. Doplniť ako výslovnú podmienku E2.1.
4. (g)/(i) E1b-1.2: doplniť invariant a test „pád po `Promise.allSettled` pred jediným checkpointom → replay vydá 0 provider HTTP“ s odkazom na cached `dispatch:false` pre `accepted` v `prepare_v2` (`20260929200000:223–224`) a vetvu `cached` v klientovi (`client.ts:489–490`); bez neho je jediný checkpoint skupiny nepodložený.
5. (k) Procesná nezrovnalosť: súbor `.omx/plans/call-latency-repair-v2-2026-09-16.md` na disku obsahuje revíziu 1 (self-review, 16. 9.), zatiaľ čo posudzovaná revízia 4 existuje len v zadaní orchestrátora; záznam iterácií (Príloha B/C) tak nie je dohľadateľný v repozitári. Odovzdaná verzia plánu musí byť tá, ktorá prešla nezávislým kolom.
6. (k) Drobné nepresnosti citácií na opravu: `effects.ts:1181–1205` (retryPending/incident per člen) — `recordTelephonyIncident` je na `:1209`; Príloha B cituje `session-runner.ts:436` ako vnorený renew — volanie `assertOwnership(existing)` je `:437`; `transitions.ts:1207` (Architect) — `waiting: null`/`queue: null` je `:1208`; predikát `noContinuation` je `session-runner.ts:209–212` (`:213` je `bridgeObservation`).
**Kontrola odkazov:** 32 overených, 4 s nálezom.
- src/server/telephony/recording-policy-service.ts:58–73 — OK (Architect): `resolveSessionRecordingPolicy` vždy vráti politiku; tvrdenie plánu v N3/E1a.1 „bez riadku politiky policyState vráti undefined“ — WRONG
- src/server/telephony/session-runner.ts:469–471 — OK (Architect): `motorist_session_lease_release_v2` vo `finally`; v Prílohe A plánu chýba — WRONG (počet)
- supabase/migrations/20260929200000_fenced_telephony_commands.sql:245 — OK: `result_v2` `perform 1 … for update`; plán `:243` — WRONG (`begin`)
- .omx/plans/call-latency-repair-v2-2026-09-16.md na disku — WRONG: obsahuje revíziu 1, nie posudzovanú revíziu 4

## Posudok Critica — revízia 4 plánu opravy odozvy hovorov (iterácia 5)

**Verdikt: ITERATE.** Plán je vecne silný a takmer všetky kódové tvrdenia sedia (overil som vyše 40 skupín citácií v snapshote `f8cb071`, zoznam v `spot_checks`). Nemôže však byť schválený, kým obsahuje tri overené faktické chyby, ktoré Architekt v iterácii 5 správne označil, a kým nezapracuje alebo výslovne neodmietne jeho šesť požiadaviek.

### Čo som potvrdil
- **Anatómia kritickej cesty** (Príloha A) sedí krok po kroku: predikát `noContinuation` (`session-runner.ts:209–212`), probe/acquire/vnorený renew (`:442, :452, :437` cez `event-processor.ts:315 → session-runner.ts:485`), checkpoint po každom efekte (`effects.ts:211–222`), 5–6 round tripov na príkaz (`:1365`, `provider-journal.ts:31–36`, `client.ts:488/501`, `:703–706`, `:1480`), guard v stage RPC (`20260928110000:44–52`, `effects.ts:1266, 1779`). `hangupCmd` (`transitions.ts:342`) legy nepatchuje, takže krok 11 = 16 je správne.
- **N10 (tichá čakáreň aj tichá fronta)**: `fork()` z pôvodného riadku (`:136–142`), `mohIsPlaying` (`:968–972`), `enterWaiting` (`:1001, 1007–1008, 1015–1019, 1020–1022`) — obidva varianty potvrdené; kompenzácia bez `playback_stop` a `gather_stop` len mimo fronty je správna.
- **N11/N12**: `FOR UPDATE` v fence (`:27`), acquire (`:113`), prepare (`:216`), presence (`20260928100000:83`); fence porovnáva len hlavičky a `motorist.telephony_internal` číta iba trigger (`:43`) — dizajn O-A/O-B/O-C je konzistentný.
- Lean kontext: cesta `call.answered` (`onAnswered :604`, `onLegAnswered :683`, `onOfferAnswered :1169`, `gatherCmd :355`) z `ctx` číta len `mediaAvailable`, `settings.parkMaxMinutes`, `now`, `recordingPolicy` — invariant E1a.1 platí.
- Hypotézy sú označené, čísla OpenAI atribuované (`telephony-latency-audit-2026-09-11.md:64, 119`), obmedzenie self-review v iterácii 1 priznané; obmedzenia repozitára dodržané; pre-mortem má presne tri scenáre; §7 a §8 sú testovateľné so vzorkami.

### Čo treba opraviť (podrobne v `issues`)
1. **N3/E1a.1 — nesprávny mechanizmus.** `resolveSessionRecordingPolicy` (`recording-policy-service.ts:58–73`) vždy vráti politiku, plná cesta ju vždy načíta (`session-runner.ts:238–253`); `policyState` (`recording.ts:28–32`) vráti undefined iba pre `internal`. Navyše `:33–39` zmrazí `enabled:false` aj pri zapnutej živej politike bez štartovacích hlášok. Dôsledok je pre plán priaznivý (`noContinuation` pokrýva prakticky celú hlavnú cestu), ale text porušuje princíp 5. Predpoklad nahradiť meraním podielu sessions so zmrazenou vypnutou politikou (nový dotaz K).
2. **Príloha A — chýba `lease_release_v2`** (`session-runner.ts:469–471`, pred `event-processor.ts:332`) a `recoverTelephonyIncidentThrottled` (`effects.ts:1679`, 0–1): súčet ≈ 61–63; §8 základ a ciele prepočítať.
3. **`result_v2` `for update` je `20260929200000:245`**, nie `:243`.
4. **Sekvenčný jediný checkpoint po kritickej dávke patrí do E1a** — argument idempotencie (`effects.ts:263–272, 227–229, 323–336`) plán už prijíma v E1b-2.3 a sekvenčná verzia neotvára N11/N5. Pozor na aritmetiku: 6 checkpointov → 1 ušetrí 5, teda 15 → 10 a `bridge` po ≈ 21 (Architekt uvádza 20). Crash-replay test je povinný.
5. **E2 prerekvizita**: `createTelephonyDeps` (`runtime.ts:69–74`) číta live gate pred vytvorením Telnyx klienta; bez lenivej konštrukcie po admit cieľ ≈ 5–6 neplatí.
6. **E1b-1.2**: doplniť test „pád po `allSettled` pred checkpointom → 0 provider HTTP“ (`20260929200000:223–224`, `client.ts:489–490`).
7. **Proces**: súbor plánu na disku je revízia 1; revízia 4 existuje len v zadaní — záznam iterácií musí byť v repozitári.
8. Drobné citácie: `effects.ts:1181–1205` (incident je `:1209`), `session-runner.ts:436` → `:437`, `transitions.ts:1207` → `:1208`.

Po zapracovaní bodov 1–7 (bod 8 je kozmetický) odporúčam APPROVE; žiadny z nálezov nespochybňuje rozhodnutie, poradie etáp ani invarianty kontraktu 2.

## Iterácia 5 (orchestrátor)

Zvyšných 6 požiadaviek Architekta v4 a 6 výhrad Critica v4 overené v kóde a zapracované do revízie 5; jedna citácia (`:245`) odmietnutá — `perform … for update` v `result_v2` je na riadku 243. Dispozície v Prílohe C plánu. Táto iterácia nemá nezávislý verdikt.
